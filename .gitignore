# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Secrets and local overrides
.env
.env.*
!.env.example
*.pem
*.key

# 로컬 전용 — 저장소 자신의 이슈 로그는 공개하지 않는다 (공개 기록은 CHANGELOG.md)
/issues.md

# 로컬 전용 — 세션 간 작업 인계 문서 (완료되면 CHANGELOG·STATUS로 흡수하고 지운다)
/handoff-*.md

# Build / dependency outputs
node_modules/
dist/
build/
__pycache__/
.pytest_cache/
.venv/
venv/
manyfast_reference/
# 생성되는 HTML 리포트 — 정본은 md 다 (.claude/scripts/report.py 가 매번 다시 만든다)
docs/reports/

# 로컬 전용 — /kit-review 통과 도장 (review-stamp.sh 가 쓰고 .githooks/pre-commit 이 대조한다)
/.claude/.kit-review-pass

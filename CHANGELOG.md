# Changelog

이 파일은 방법론·템플릿·가이드의 변경 이유와 적용 범위를 기록합니다. 제품별 기능 변경은 각 제품 저장소가 기록합니다.

## 0.7.0 — 2026-08-27

**키트가 넣는 커맨드·서브에이전트 이름을 전부 `mdm-` 으로 시작하게 했습니다. 깨지는 변경입니다.**

키트가 제품 저장소에 넣던 슬래시 커맨드 이름은 `/plan`·`/review`·`/stage`처럼 흔한 말이었습니다.
그 저장소가 이미 쓰던 커맨드나 다른 플러그인이 넣는 커맨드와 이름이 겹치면 **어느 쪽이 뜨는지 알 수 없고**,
키트 문서가 "`/review` 를 돌린다"고 적어도 그 문장이 키트의 것을 가리킨다는 보장이 사라집니다.
이름 공간을 하나 차지하는 쪽을 골랐습니다 — 이 키트가 넣는 것은 커맨드든 서브에이전트든 전부 `mdm-` 으로 시작합니다.

### 개명

| 옛 이름 | 새 이름 |
|---|---|
| `/adopt` | `/mdm-adopt` |
| `/plan` | `/mdm-plan` |
| `/ready` | `/mdm-ready` |
| `/review` | `/mdm-review` |
| `/stage` | `/mdm-stage` |
| `/cycle-close` | `/mdm-cycle-close` |
| `/ingest-errors` | `/mdm-ingest-errors` |
| `code-review` 서브에이전트 | `mdm-code-review` |
| `error-learning` 서브에이전트 | `mdm-error-learning` |

방법론 저장소 자신의 것도 같이 바꿨습니다: `/kit-review` → `/mdm-kit-review`,
리뷰 축 에이전트 `kit-review-k1`~`k6` → `mdm-kit-review-k1`~`k6`, 반증 에이전트 `kit-refute` → `mdm-kit-refute`.

### 업그레이드 — **설치기는 옛 이름을 건드리지 않습니다**

`scripts/install-kit.sh <저장소> --upgrade` 는 옛 이름 파일을 **찾아서 ⚠ 로 알리기만** 합니다.
지우지도, 개칭하지도 않습니다. 처리는 `templates/dev-kit/README.md` 「업그레이드」 **4-1** 의
손 절차로 **사람이** 합니다 — 복사해 쓸 수 있는 명령을 그 자리에 두었습니다.

**자동 이관을 두 판 만들었고 이 저장소의 리뷰가 둘 다 치명으로 잡았습니다.**
1판 `rm -f` — 버전·소유권 확인이 없어 프로젝트 자기 파일을 지웠고, 설치기가 신규 설치에서
화면에 인쇄하는 「그 저장소 자신의 것이면 그대로 둬도 된다」를 다음 업그레이드가 거짓으로 만들었습니다.
2판 개칭 + 가드 넷 — 버전 가드가 `CLAUDE.md` 스탬프에 의존하는데 업그레이드는 그 파일을 갱신하지
않으므로(`CLAUDE.md.dev-kit-new` 만 씁니다) 가드가 **실제 경로에서 한 번도 닫히지 않았습니다.**
그때 회귀 fixture 는 손으로 스탬프를 올려 **실제 경로가 만들 수 없는 상태**를 재고 있었습니다.

뿌리는 하나였습니다 — **설치기는 「이 파일이 키트 것인가」를 알 방법이 없습니다.**
그 판단이 필요한 일은 스크립트가 하지 않습니다. 파일을 열어 자기 것인지 아는 사람이 하는 것이 맞습니다.
(대가: 업그레이드하는 사람이 손으로 한 단계 더 합니다. 설치기가 무엇을 해야 하는지 정확히 알려 줍니다.)

**소유권 표를 정정했습니다**: `.claude/` 는 「전체 키트 소유」가 아니라 **키트가 만든 파일만** 키트 소유입니다.
`settings.json` 은 키트가 만들었지만 프로젝트가 자기 훅을 더할 수 있어 **교체하지 않습니다**(선재 동작을 표에 명시).
그리고 `docs/plan/index.md`·`docs/decisions/index.md` 가 **프로젝트 소유로 내려왔습니다** —
그 두 표에 사이클 현황 행·ADR 목록 행이 실리는데, 업그레이드가 양식으로 통째 교체하면 그 행들이 사라집니다.

### 양식 변경 (프로젝트 소유 파일 — 손으로 옮겨 적으십시오)

위 강등으로 아래 두 파일은 이제 업그레이드가 갱신하지 않습니다. **0.7.0 양식에서 달라진 것**은 이것뿐이니
기존 내용을 지우지 말고 다음 문장만 더하십시오 (원본은 `templates/dev-kit/docs/…` 에 있습니다):

- **`docs/plan/index.md`** 「사이클 현황」 표 — `#` 칸에 **파일명의 사이클 ID(`C01`)를 그대로** 적습니다.
  순번(`1`)을 적으면 정합성 검사 I 가 「표에 행이 없다」로 실패시킵니다.
- **`docs/decisions/index.md`** 「목록」 표 — `#` 칸에 **`ADR-<nnn>`** 을 그대로 적습니다.
- (참고) **`docs/status/STATUS.md`** 「활성 병렬 작업」 표 — `ID` 칸에 **`ST-<nnn>`**. 이 파일은 원래
  업그레이드 대상이 아니었으므로 강등의 대가가 아니지만, 검사 I 가 같은 형식을 요구합니다.

같은 규칙을 `docs/index.md`(업그레이드가 교체합니다)가 완전한 형태로 담고 있으니, 옮겨 적지 못했더라도
등재 절차 자체는 그 파일에서 읽을 수 있습니다.

업그레이드한 저장소가 자기 문서(**`CLAUDE.md` 포함**)에 옛 이름을 적어 두었다면 손으로 고쳐야 합니다.
설치 스크립트가 마지막에 찾는 명령 **세 줄**을 알려 줍니다 — 앞 둘은 **경로에 걸리지 않게 앞뒤를 제한한 것**이고
(옛 판은 `docs/plan/` 같은 디렉토리를 100건 넘게 잡아 신호가 묻혔습니다), 셋째는 `commands/adopt.md` 같은
**파일 경로 형태**를 잡습니다.

**개명 후에는 세션을 다시 시작해야 새 이름이 잡힙니다** — 커맨드·에이전트 목록은 세션 시작 시점에 읽힙니다.

### 검사

- **`scripts/test-install-upgrade.sh` 신설**(CI, 13 케이스 + 뮤테이션 자기검증 1) — 임시 저장소에
  0.6.0 배포본을 합성해 깔고 실제로 설치·업그레이드를 돌립니다. 재는 것은 **불간섭**입니다:
  옛 이름 9개의 체크섬이 업그레이드 전후로 같은가, 개칭 흔적도 없는가, 대신 알리는가,
  손 절차로 안내하는가, 신규 설치와 업그레이드가 **같은 약속**을 지키는가, 카탈로그 행이 보존되는가.
  **0.6.0 합성이 파일명뿐 아니라 문서 본문의 커맨드 이름까지 되돌립니다** — 이름만 되돌린 가짜 0.6.0 은
  본문이 이미 새 이름이라 「업그레이드가 키트 문서를 갱신하는가」를 구조적으로 못 잽니다.
  **뮤테이션 자기검증**은 파괴적 이관을 되살리면 불간섭 케이스가 붉어지는 것을 확인하고,
  그 전에 **뮤턴트가 실제로 돌았는지**(rc·출력)를 먼저 단언합니다 — 뮤턴트가 죽으면
  「안 건드렸다」가 저절로 참이 되기 때문입니다.
  (루트 CLAUDE.md 「검사를 새로 넣으면 그 검사가 붉어지는 fixture를 같이 넣는다」)
- **`check-docs.py` 검사 11 신설** — 키트가 배포하는 `.claude/{commands,agents}/**/*.md` 의 파일명과
  frontmatter `name:` 이 전부 `mdm-` 으로 시작하는지 봅니다(**하위 디렉토리까지**). 위의
  "전부 `mdm-` 으로 시작합니다"가 약속으로 남지 않게 하는 장치입니다.
  `scripts/test-docs-check.sh` 에 붉어지는 fixture 4케이스를 함께 넣었습니다.
- `scripts/check-docs.py` 검사 7 의 축↔에이전트 대조가 새 이름을 기준으로 봅니다.

이 CHANGELOG 의 **0.6.0 이하 항목은 그때의 이름 그대로 둡니다** — 그때의 사실이 그것이기 때문입니다.
(경로가 깨지는 백틱 참조 한 자리만 새 이름으로 고쳤고, 그 문단에 옛 이름이 함께 남습니다.)

## 0.6.0 — 2026-08-27

**검사에도 검사를 붙였습니다.**

0.5.0 까지 이 키트는 "규칙을 스크립트가 강제한다"고 말해 왔습니다. 이번 판에서 그 문장의 한 층 아래를 봤습니다 —
**그 스크립트가 실제로 무엇을 잡는지는 누가 확인하나.** 답은 "아무도"였습니다.
새로 넣은 검사 셋이 아무것도 못 잡는 상태로 커밋 직전까지 왔고, 문서에는 "손으로 확인한다"고 적혀 있었습니다.
손으로 한 확인은 저장소에 남지 않아 다음 변경에서 아무것도 막지 못합니다.

그래서 규칙이 하나 늘었습니다: **검사를 새로 넣으면 그 검사가 붉어지는 fixture 를 같이 넣는다. 예외 없다.**
그리고 절대 규칙 3 의 후단("못 만들면 권고로 고쳐 쓴다")은 **정말 못 만들 때만** 씁니다 —
만들 수 있는데 안 만들고 한계를 적는 것은 규칙을 지킨 것이 아니라 우회한 것입니다.

### 검사 (백로그 정리 — 약속을 검사로 바꾸거나, 못 바꾸면 권고로 내립니다)

- **검사에도 검사가 생겼습니다** (`scripts/test-docs-check.sh`, CI 등록, 30 케이스). 문서 검사기의
  **검사 1-c·5·6·7·8·9·10 에 위반을 심어 실제로 붉어지는지** 확인합니다
  (검사 1·1-b·2·3·4 는 아직 덮지 못했습니다 — 그 사실을 여기 적어 둡니다). 이것이 없던 동안 새로 넣은 검사 셋이 **아무것도 못 잡는
  상태**로 커밋 직전까지 왔습니다 — 로케일에 따라 통째로 무력해지거나, 추출이 0건이 되면 조용히 "통과"를
  내거나, `tools:` 표기를 바꾸면 대상에서 빠졌습니다. 처음 실행에서 16개가 실패했습니다.
  **exit code 만 보지 않고 기대 메시지까지 단언합니다** — 다른 이유로 붉어진 것을 "잡았다"로 세면
  검사가 있는 척만 하게 됩니다.
- **문서·에이전트 검사가 로케일에 흔들리지 않습니다** (`scripts/check-docs.py`).
  `grep` 의 부정 문자클래스는 C/POSIX 로케일에서 **바이트 클래스**가 되어 한글·em dash 안의 바이트에
  걸립니다. 그러면 추출이 0건이 되고 검사는 아무 일 없다는 듯 통과했습니다.
- **셸 함정 lint** — 이 저장소가 실제로 밟은 둘을 검사가 막습니다:
  `$변수` 뒤에 곧바로 오는 멀티바이트 문자(bash 가 그 바이트를 변수명에 붙여 읽어 `set -u` 아래에서 죽습니다)와
  awk 의 비ASCII `==` 비교(BSD awk 는 비ASCII 문자열 둘을 무조건 같다고 판정합니다).
  **주석은 재발을 못 막습니다** — 후자는 이미 주석으로 경고가 적혀 있었는데도 그대로 밟았습니다.

- **`check-docs.sh`가 이 저장소 자신을 보게 됐습니다.** 지금까지 백틱 경로 검사는 `templates/dev-kit` 안만
  봤습니다 — 리뷰 시스템 문서(`CLAUDE.md`·`.claude/**`)가 통째로 **무검사 지대**였습니다.
  넣자마자 **경로 관례 위반 6건**을 잡았고(`docs/` 접두어 누락), 검사를 약화시키는 대신 참조 쪽을 고쳤습니다.
- **「절 이름」 포인터에 검사가 생겼습니다.** 이 키트의 정본 구조는 파일 경로가 아니라
  `docs/guides/ready.md` 「Story 크기 — 어디서 멈추나」처럼 **절 이름**으로 가리켜집니다(21곳).
  절 제목을 바꾸면 정본 구조가 **조용히** 끊겼습니다. 이제 그 이름이 대상 파일의
  헤딩·굵은 라벨·표 칸에 실재하는지 대조합니다. `report.py`가 하드코딩한 절 이름도 같은 방식으로 봅니다.
- **`/kit-review` 축↔에이전트 대응** — 표에만 있고 파일이 없으면 리뷰가 뜨는 순간 실패하고,
  파일만 있고 표에 없으면 그 축은 안 돌아갑니다. 파일명↔frontmatter `name` 불일치도 봅니다.
- **`scripts/test-report.sh` 신설** (CI 등록) — `report.py`가 Story 문서를 하나라도 찾으면
  사이클 문서의 축약 슬롯을 **안 보던** 결함을 고치고, 그 결함이 되살아나면 붉어지는 fixture를 함께 만들었습니다.
  Story 문서와 축약 슬롯은 배타가 아니라 **공존**합니다 — 어떤 항목이 문서를 갖는지는 프로파일이 정하고,
  Standard는 일부만 갖기 때문입니다.
- **정합성 검사의 `준비` 칸 4분기에 회귀 fixture** — 실패 3분기(빈 칸·재판정·❌)와 경고 1분기,
  그리고 **등급이 뒤바뀌지 않는지**까지 실측합니다. 열이 아예 없으면 조용히 꺼지지 않고 알린다는 것도 함께.

### 변경

- **"누가 Story 문서를 갖나"의 정본이 하나가 됐습니다.** `docs/guides/profiles.md` 「Story 문서」 행이
  프로파일과 속성(권한 영향·병렬)을 **함께** 정하고, 나머지 4곳은 가리키기만 합니다.
  전에는 한쪽이 프로파일로, 다른 쪽이 속성으로 말해 `Standard`+`병렬` 같은 조합에서 답이 갈렸습니다.
- **`code-review` 서브에이전트에 검사 17 「기록 없이 고치기」** — 이번 diff에 버그 수정이 있는데
  `docs/quality/issues.md`에 기록이 없으면 지적합니다. 조용히 고친 버그는 승격 카운터에 안 잡혀
  같은 유형이 세 번째에도 규칙이 되지 않습니다. **스크립트로 판정하지 않는 이유**도 함께 적었습니다 —
  커밋 패턴 감지는 리팩터링을 수정으로 오인합니다.
- **반증 단계에 「기계 장치가 없다」를 명시했습니다.** *"치명·높음은 반증을 거쳐서만 확정된다"*는
  강제가 아니라 **절차**입니다 — 이 하네스에 "서브에이전트가 실제로 떴다"를 관측하는 자리가 없습니다.
  대신 관측되는 흔적 하나를 규정했습니다: 치명·높음 행마다 반증 결과를 적고, **안 적힌 행은 반증을 안 거친 것으로 읽습니다.**
- **Write를 가진 리뷰 에이전트의 임시 디렉토리 제한**이 사라지지 않는지 검사합니다.
  샌드박스가 없으므로 경로 제한 자체는 끝까지 절차이고, 검사가 보장하는 것은 그 문장의 존속까지입니다.

### 추가

- **Story 층에 크기 기준이 생겼습니다.** 마일스톤에는 나누는 신호 4개가, 사이클에는 3개가 있었는데
  **Story만 없었습니다.** 정의도 *"한 에이전트가 끝까지 책임지는 가장 작은 단위"*라 순환이었고,
  어디까지 쪼개고 **어디서 멈추는지**를 말하는 곳이 키트 전체에 없었습니다.
  - **정본은 `docs/guides/ready.md` 「Story 크기 — 어디서 멈추나」** 한 곳입니다.
    `docs/plan/index.md` 원칙 6과 `docs/guides/S6-build.md` 1절은 **가리키기만** 합니다.
  - **크기를 미리 정해 놓고 자르지 않습니다.** 한 문장(`<누가>가 <트리거>하면, 시스템이 <동작>하여 <결과>가 된다`)이
    **트리거 하나·종료 상태 하나**로 써지고 정상·거부·오류 경로를 함께 검증할 수 있으면 거기서 멈춥니다.
  - **나누지 않는 것 6가지**를 함께 못 박았습니다 — 정상/오류 경로, 권한 **허용/거부**, 검증/안내, 저장/표시,
    같은 행동의 테스트, UI·API·DB 계층. 이건 취향이 아니라 **양식이 이미 요구하던 것**입니다:
    `ST-000` 4절이 권한 거부 경로를 필수로 요구하므로, 거부를 떼어 내면 첫 Story의 검증 표를 채울 수 없습니다.
  - **기계 장치는 없고, 표에 그렇게 적었습니다** (절대 규칙 3의 후단 — 못 만들면 권고로 적는다).
    제목의 "및/또는"을 세는 검사는 **일부러 만들지 않았습니다**: *"권한 확인 및 파일 선택"*이 정상 사례라
    설계상 오탐이 정상인 검사가 되고, 무시하라고 만든 경고는 다른 경고까지 무디게 만듭니다.
- **`/ready`에 2단계 「크기 판정」** — 슬롯 12칸은 *모호함*만 판정하므로 트리거 칸에 트리거를 셋 적어도
  전부 `✅`로 통과했습니다. 위치는 **초안 뒤·질문 앞**입니다 — 트리거·결과 슬롯이 판정의 입력이라
  초안 없이는 트리거를 셀 수 없습니다. 쪼개져 나온 Story는 판정된 적이 없으므로 **1단계로 되돌립니다.**
- **크기 판정은 프로파일과 무관합니다.** Story 문서는 Lite가 안 만들고 Standard도 권한 영향분만 만들므로,
  `ST-000`에만 걸면 대부분의 경로에서 없는 규칙이 됩니다. `C00-template.md`의 「이번에 만들 것」·
  「포함 Story」·「시작 전 확인」에 함께 걸고 **"문서를 안 만드는 것이지 판정을 건너뛰는 것이 아니다"**를 명시했습니다.
- **상류의 계층을 옮겨 오지 않습니다** (`docs/plan/index.md` 원칙 8) — 계획 도구가 몇 층으로 나누든
  그 층은 키트의 층이 아니고, **상류의 최하위 항목이 곧 Story인 것도 아닙니다.** 멈추는 지점은 크기 판정이 정합니다.

- **마일스톤 층에 절차와 강제 장치가 생겼습니다.** 지금까지 마일스톤은 `roadmap.md`의 **빈 표**였습니다 —
  누가 언제 긋는지, 무엇을 기준으로 나누는지 말하는 곳이 한 군데도 없었고, `/cycle-close`가 "끝났으면 상태를 갱신한다"고만 했습니다.
  Epic 계층을 "17곳에서 참조되고 0곳에서 정의됨"으로 잘라냈는데, 마일스톤은 참조가 적었을 뿐 구조가 같았습니다.
  - **`/adopt` 7단계 「마일스톤 배치」 신설** (`docs/guides/S0-adopt.md` 7절이 정본).
    **상류가 주지 않는 축이라 키트가 세웁니다** — 계획 문서는 "무엇을"의 트리이지 "언제까지 무엇이 되나"가 아닙니다.
    배치 전에 의존 순서("A 없이 B가 성립하나")를 훑습니다.
  - **행은 처음에 전부, 내용은 열 때.** 모든 요구사항을 마일스톤에 배치하고 마일스톤 행도 전부 만들되,
    **「무엇이 되면 달성인가」와 목표 시점은 M1 것만** 채웁니다. 나머지는 `/cycle-close` 9-1단계가 앞 마일스톤을 닫을 때 채웁니다 —
    돌아보지 않은 마일스톤의 달성 조건은 계획이 아니라 추측이고, 그 칸이 안 써진다는 사실 자체가 분할이 틀렸다는 신호입니다.
  - **백로그로 보내지 않습니다.** 백로그는 "범위 밖에서 떠오른 것"의 자리입니다. 계획에 이미 있는 요구사항을 거기 보내면
    꺼낼 때 S1부터 다시 밟게 됩니다. 아직 착수 안 한 요구사항의 자리는 **매핑표의 `⬜ 대기` 행**입니다.
  - **나누는 신호 4개** (`docs/plan/index.md` 핵심 원칙 5가 정본, 사이클 크기 신호 옆자리):
    달성 조건을 한 문장으로 못 씀(사람) · 사이클 5바퀴 초과(검사) · 요구사항 1개(검사) · 배포 없이 끝난 사이클(권고, 검사 없음).
    **어느 신호를 기계가 잡고 어느 것을 못 잡는지 표에 명시**했습니다 (절대 규칙 3).
- **매핑표에 `마일스톤` 열** (`docs/spec/source-map.md` 2절) — 배치가 요구사항의 정본 자리에 함께 삽니다.
  검사기가 열 이름으로 찾으므로 열이 늘어도 기존 검사는 깨지지 않습니다.
- **정합성 검사 H — 마일스톤 배치** — 배치가 빈 요구사항은 **실패**, 크기 신호 둘은 **경고**.
- **`scripts/test-consistency.sh` + CI** — 검사 H의 회귀 fixture. 심는 결함마다 기대 신호를 요구하고,
  마지막에 **H 블록을 들어내면 fixture 가 깨지는지**를 확인하는 뮤테이션 자기검증을 붙였습니다.
  이 fixture 가 작성 중에 실제 버그를 잡았습니다 — `$(... | sort -u)`가 후행 개행을 지워 마지막 줄이 카운트에서 빠졌고,
  그래서 6바퀴가 5로 세어져 **검사가 느슨해지는 방향으로** 틀렸습니다.
- **커밋 정책 — 승인 모드 / 보고 모드** (`docs/guides/commit-policy.md`가 정본, 절대 규칙 13, `product.md`·`STATUS.md`에 기록).
  지금까지 키트에는 **커밋에 관한 규칙이 한 줄도 없었습니다** — 누가 언제 커밋하는지, 사용자에게 묻는지가 미정의였습니다.
  - **판정을 강요하지 않고 관여 수준을 고르게 합니다.** "이 리뷰 지적이 커밋을 막을 만한가"는 개발 경험이 있어야 답할 수 있어서,
    비개발자에게 매번 물으면 뜻 없이 전부 승인하거나 무서워서 멈춥니다 — 둘 다 관여가 아닙니다.
  - **묻는 방식을 규정했습니다.** `ST-003 구현 완료, 커밋할까요?`가 아니라
    `로그인 화면을 만들었습니다. 리뷰는 통과했고 낮음 1건이 남았습니다. 커밋할까요?` —
    **무엇이 바뀌었나·리뷰 결과·남은 지적**을 함께 주지 않으면 판정이 아니라 형식이 됩니다. 보고 모드의 보고도 같은 재료를 담습니다.
  - **모드와 무관한 것**: `/review` 통과 없이 커밋하지 않는다 ·
    push·배포·태그·**이력 재작성**·되돌릴 수 없는 외부 행동은 반드시 승인받는다. 이 목록의 정본은 `commit-policy.md` 한 곳입니다.
  - S1에서 프로파일과 함께 묻고 DoD `(사람 확인)` 항목이 게이트입니다. **기계 장치는 없고, 없다고 문서에 적었습니다** (절대 규칙 3).
- **`code-review` 서브에이전트에 "실행이 증거다"** — 재현 가능한 지적은 실제로 재현해 커맨드를 근거로 남기고,
  테스트 실질성은 이름·개수가 아니라 **검사 명령을 직접 돌린 출력**으로 확인합니다(전달받은 초록불을 믿지 않습니다).
  훅·검사 스크립트가 범위에 있으면 읽지 말고 우회 입력으로 돌려 봅니다.
- **재리뷰 반복 상한 3회전** (`/review`·`/cycle-close`) — 최초 리뷰가 1회전, 수정→재리뷰가 한 회전씩이고
  `issues.md`에 회전 수를 명시합니다. 3회전 뒤에도 치명·높음이 남으면 검사를 약화시키지 않고 ⛔로 멈춥니다 —
  3회전으로 닫히지 않는 문제는 수정이 아니라 설계 판단이 필요하다는 신호입니다.

### 수정

- **회귀 검사가 남의 임시 디렉토리를 지우던 것**을 고쳤습니다. 게이트 회귀 검사가 공용 임시 디렉토리를
  통째로 스냅샷해, 검사가 도는 몇 초 사이에 **다른 프로세스가 만든 디렉토리를 누수로 오인하고 삭제**했습니다.
  도장 스크립트의 임시 디렉토리에 `kit-stamp.` 접두어를 줘서 **내 것만 세고 남의 것은 건드리지 않습니다.**
- **문서 검사가 「절 이름」 개명을 놓치던 것**을 고쳤습니다. 오탐 하나를 막으려 "인용한 문서 자신의 라벨도
  인정"하는 완화를 넣었는데, 그것이 검사를 통째로 뚫었습니다. 완화를 걷어내고 **포인터 형태를 좁혔습니다**
  (괄호·쉼표가 끼면 포인터가 아니라 서술문입니다).
- **셸 함정 lint 가 여러 줄 awk 를 못 보던 것**을 고쳤습니다 — 이 저장소의 awk 는 대부분 여러 줄이고,
  하필 그 함정을 처음 밟게 한 코드가 정확히 그 모양이었습니다.
- **회귀 fixture 가 케이스마다 격리되지 않던 것**을 고쳤습니다. 30여 개 케이스가 한 임시 디렉토리를
  공유해 앞 케이스가 만든 파일이 뒤로 샜습니다 — 통과가 격리 덕이 아니라 **순서 덕**이었습니다.
- **"각 분기에 위반을 심어 확인합니다"를 실물에 맞춰 좁혔습니다.** 실제로 덮는 것은 검사 1-c·5~10 이고,
  검사 1·1-b·2·3·4 는 아직 증거가 없습니다 — **그 사실을 문서에 적었습니다.** 특히 훅 실행 권한 검사는
  스스로 "유일한 그물"이라 적은 자리인데, 그것을 통째로 꺼도 fixture 가 전부 초록이었습니다.

- **GitHub 저장소 description** 을 채웠습니다 — 공개 저장소인데 첫 화면에 무엇인지가 안 나왔습니다.
- **도장 스크립트의 임시 인덱스 누수·TOCTOU** — `mktemp`로 파일을 만든 뒤 지우고 그 이름을 git에 넘기던 것을
  `mktemp -d`(0700) 안으로 옮기고 **`kit-stamp.` 접두어**를 줬습니다 — 공용 임시 디렉토리에서
  내 것을 남의 것과 구별하기 위해서입니다. 시그널 정리가 동작하려면 트랩을 **명령치환 서브셸 안**에
  걸어야 합니다(밖에 걸면 부모 핸들러가 빈 값을 봅니다). 게이트 회귀 검사가 SIGINT 로 끊어 실측합니다 —
  트랩을 지우면 붉어집니다.
  신호 목록을 **EXIT 하나로** 둔 것은 관용이지 실측된 결함 대비가 아닙니다:
  이 스크립트에서는 `EXIT` 와 `EXIT INT TERM HUP` 이 **관측 가능한 차이를 내지 않습니다**(둘 다 rc 130).
  잴 수 없는 것을 재는 척하지 않으려고 그 케이스는 두지 않았습니다.
- **사이클 크기 신호의 정본이 둘이던 것을 하나로** (`docs/plan/index.md` 원칙 4가 정본).
  `docs/guides/S6-build.md` 1절의 복제 목록을 지우고 포인터로 바꿨습니다 — 같은 문서 3-2절이
  "금지 목록을 여기 복사해 두지 않는다"고 하면서 바로 위에서 사이클 신호를 복사하고 있었습니다.
- **`ST-000` 1절의 한 문장 형식**을 `<누가>가 <무엇>을 할 수 있게 된다`에서
  `<누가>가 <트리거>하면, 시스템이 <핵심 동작>하여 <확인 가능한 결과>가 된다`로 바꿨습니다 —
  크기 판정이 보는 것(트리거 하나·종료 상태 하나)이 문장에 드러나야 판정이 됩니다.

### 이 저장소 자신의 개발 규칙 (배포물 아님)

- **`/kit-review`** — 이 저장소의 변경은 커밋 전에 축별 서브에이전트 리뷰를 거칩니다.
  K1 약속–강제 대조 · K2 우회 재현(실행) · K3 셸 정확성 · K4 수명주기 · K5 문서 정합 · K6 회귀 증거,
  그리고 치명·높음은 `kit-refute` 반증을 거쳐서만 확정됩니다. **축마다 다른 에이전트를 씁니다.**
- **커밋 게이트** — `.githooks/pre-commit`·`pre-merge-commit`이 커밋될 트리가 리뷰 통과 도장(git write-tree)과
  일치할 때만 커밋을 허용합니다. **에이전트 셸에서만** 겁니다 — 사용자는 리뷰 지적의 반영 여부를 정하는 권한자이지 통제 대상이 아닙니다.
  못 막는 것(`--no-verify`·config override·cherry-pick/revert/rebase·표식 비우기)을 **한계로 열거**했습니다.
- **`scripts/test-review-gate.sh` + CI** — 게이트의 회귀 fixture. 이전에 뚫렸던 우회를 그대로 심어 봉쇄를 실증하고,
  마지막에 게이트를 꺼서 fixture 가 붉어지는지 확인하는 뮤테이션 자기검증을 붙였습니다.

## 0.5.0 — 2026-08-26

키트의 위치를 바꿉니다. **계획을 뽑아내는 일은 상류가 하고, 이 키트는 그다음을 맡습니다** —
이미 있는 계획 문서를 받아 **구현 중에 문서가 서로 어긋나서 생기는 오류와 일탈을 막는 것**.

0.3.0까지 모든 경로는 `/stage 1` 인터뷰에서 시작했고, 문서 정합성 대조는 `/cycle-close`에서 사람이 눈으로 읽는 것이 전부였습니다.
계획 도구가 이미 있는 사용자에게는 S1~S5가 중복 의식이었고, 정합성은 여전히 사람 몫이었습니다.

그리고 **어떤 계획 도구도 전제하지 않습니다.** 특정 SaaS가 있어야 돌아가는 방법론은 그 SaaS가 없는 사람에게는 없는 것과 같습니다.
계획이 없으면 키트가 직접 만듭니다.

### 추가

- **S0 도입 단계 + `/adopt` 커맨드** — 모든 프로젝트의 진입점. 다섯 가지를 합니다:
  ① **찾기** — 저장소 안(`docs/`·`doc/`·`spec/`·루트 `*.md`, 이름 패턴 `prd·plan·requirement·brief` 등)을 **사용자에게 묻기 전에** 먼저 뒤집니다. 없으면 밖에 있는지 묻고, 그래도 없으면 `/plan`으로 보냅니다
  ② 수집(→ `docs/upstream/` 스냅샷 + 해시)
  ③ **교차 대조** — 따로 생성된 계획 문서는 반드시 서로 어긋나 있고, 그걸 도입 시점에 판정하지 않으면 구현 중에 "어느 문서가 맞나"로 매번 멈춥니다
  ④ **ID 정리** — 발급이 아니라 **인용**입니다. 아래 참조
  ⑤ **계약 확인** — 상류가 무엇을 담고 무엇을 안 담는지 확인합니다. 아래 참조
- **`/plan` 커맨드 + `docs/guides/plan.md`** — 계획 문서가 아예 없을 때 키트가 직접 만듭니다.
  압박(아이디어가 버티는가) → 묶어 묻기 → 작성 → 검증 4단계. **"기각"이라는 출구가 있습니다** — 안 버티는 아이디어를 잘 정리해 봐야 잘 정리된 채로 실패합니다.
  산출은 `docs/upstream/plan.md`로, **키트가 만들었어도 "요구사항이 태어난 곳"의 자리는 같습니다** (`manifest.tsv` 출처만 `self:plan`).
  외부 도구와 다른 점은 **키트가 쓸 수 있는 형태**로 만든다는 것입니다 — 요구사항마다 ID와 **검증 조건 목록**, 화면 목록, 비목표, 부작용 지표.
- **`docs/spec/source-map.md`** — 요구사항·화면 정본 매핑표. 상류와 저장소를 잇는 유일한 정본입니다.
  **항목 단위 소유권 분할**(1절)이 핵심 장치입니다 — 상류와 저장소가 둘 다 갱신돼도 겹치는 곳이 없어 갈라질 수 없습니다.
  다만 **누가 무엇을 소유하는지는 미리 정해져 있지 않고 4절 계약 확인이 정합니다.**
- **`.claude/scripts/check-consistency.sh`** — 문서 정합성 기계 검사 7종. 상류 스냅샷 무결성 · 요구사항 커버리지 · **테스트 실재**(매핑표에 적힌 테스트 이름이 코드에 실제로 있는가 — 이름만 적으면 개수 검사가 속습니다. 문서 속 인용은 실재로 치지 않습니다. git 이 없는 폴백 경로에서는 `.gitignore` 를 읽을 수 없으므로 `node_modules`·`vendor`·`dist`·`build`·`target`·`.venv` 를 이름으로 빼 **설치된 의존성 안의 동명 문자열에 속지 않게** 했습니다) · 상류 변경 재검토 잔존 · 고아 ID 인용 · 화면 정합 · 참조 깨짐.
  훅이 아니라 스크립트입니다 — 매 도구 호출마다 돌면 비싸고, 구현 도중 일시적으로 어긋나는 것은 정상이기 때문입니다. `/review` 1단계 · 사이클 시작 · `/cycle-close` · CI에서 돕니다.
  **어떤 외부 도구도 부르지 않습니다** — 저장소 안 스냅샷만 보므로 상류가 죽어도, 네트워크가 없어도, CI에서도 그대로 돕니다.
- **`docs/upstream/`** — 계획 문서의 읽기 전용 스냅샷 + 기계용 수집 기록(`manifest.tsv`). 손으로 고치면 해시로 잡아 실패시킵니다.
  **문서 개수·종류를 고정하지 않습니다** — 한 장짜리 하나일 수도, 넷일 수도 있습니다.
- **`/adopt --sync`** — 상류 재수집·diff. 바뀐 요구사항에 딸린 전이표 행·권한 행·테스트·**검증 조건 개수 변화**를 함께 나열해 "재검토"로 표시하고, 처리 전까지 검사가 실패합니다. 요구사항 변경에 딸린 **파생 갱신**을 놓치지 않기 위한 장치입니다.
- **`/ready` 커맨드 + `docs/guides/ready.md`** — 구현을 시작해도 되는지 판정합니다.
  **계층이 다르면 묻는 것도 다릅니다** — 이게 설계의 핵심입니다:
  **요구사항**은 *"무엇이 되면 됐나"*(수용 기준 → 매핑표 `조건 수`), **Story**는 *"어떻게 동작하나"*(**개발 준비 슬롯 12칸**).
  하나로 뭉치면 요구사항 하나가 Story 셋으로 쪼개질 때 칸 하나에 섞여 버립니다.
  12칸 = 선행조건·트리거·동작·결과·예외·표시·권한&접근·비즈니스 규칙·데이터 + **상태 전이·검증 조건·검사 명령**.
  **값이 어디 사는지가 칸마다 다릅니다** — 여러 Story가 공유하는 것(권한·규칙·데이터·전이·표시·예외·검사 명령)은
  **정본을 가리키기만** 하고(`domain #3`), 이 Story만의 것(선행조건·트리거·동작·결과)만 Story에 직접 씁니다.
  베껴 쓰면 여러 벌이 되어 갈라지기 때문입니다.
  **채우는 건 AI, 확인하는 건 사람입니다** — 빈 칸을 전부 물어보면 빈칸 노동이 되어 다음 프로젝트에서 안 쓰입니다.
  정본·상류·코드를 읽어 초안을 채우고, **어느 쪽을 골라도 말이 되는데 고르는 순간 구현이 달라지는 것만** 질문으로 올립니다.
  매핑표 `준비` 칸은 슬롯을 다시 적는 곳이 아니라 **딸린 Story의 롤업**(`✅ ST-003,ST-004`)이고,
  진행 중인데 `❌`가 남으면 **검사기가 실패시킵니다.** Lite는 Story를 안 만들므로 사이클 문서에 축약형을 둡니다.
  **전부 `✅`가 목표가 아닙니다** — 화면 없는 Story의 표시 칸은 `—`(해당 없음 + 이유)가 정답입니다.
- **실패에는 해결 경로가 붙습니다** — 검사가 실패했을 때 어디로 가야 하는지가 어디에도 없었습니다.
  `.claude/commands/mdm-review.md`(당시 이름 `review.md`) 1단계에 **실패 유형별 해결 경로 표**를 넣고, 검사기의 각 실패 메시지에도 갈 곳을 붙였습니다
  (`고아 인용: X → 오타면 고치고, 새 요구사항이면 /adopt`). **보고만 하고 끝내지 않습니다** —
  해결이 사용자 판정을 요구하면 STATUS의 **막힌 것**에 ⛔로 올리고 멈춥니다.
- **판정은 한 번 하고 끝나지 않습니다 ★** — 판정 트리거가 도입 시점 하나뿐이었습니다.
  슬롯 판정은 *그 시점의 상류*를 근거로 내린 것이라, 상류가 바뀌면 근거가 사라집니다.
  **근거가 사라졌는데 판정만 살아 있는 것이 가장 조용한 형태의 드리프트입니다.**
  그래서 `/adopt --sync`가 상류 변경을 잡으면 그 요구사항의 `준비` 칸을 **`❌ 재판정`으로 되돌리고**,
  4절 계약 확인표의 관련 항목도 다시 확인합니다(상류가 권한을 담기 시작했을 수도, 빼버렸을 수도 있습니다).
  검사기는 재판정과 일반 미달을 **구분해서** 안내하고, `/cycle-close` 13단계가 다음 사이클 준비도를 다시 봅니다.
- **`docs/spec/domain.md` 5절 비즈니스 규칙** — 지금까지 도메인 규칙이 저장될 자리가 아예 없었습니다.
  절 구성이 시나리오·데이터·상태·권한뿐이라 *"동의를 거부하면 가져오기 자체를 막나, 전사 처리만 막나"* 같은
  약속이 갈 곳이 없었고, 그래서 구현 중에 정확히 **지어내졌습니다.**
  규칙마다 **"어기면 무슨 일이"를 함께 적습니다** — 못 적으면 규칙이 아니라 취향이므로 지웁니다.
- **`.claude/scripts/report.py` — HTML 리포트** — md를 읽어 보기 쉬운 한 장으로 그립니다.
  `/adopt` 도입 결과 · `/plan` 계획 결과 · `/ready` 준비도 · `/cycle-close` 사이클 결과 네 시점,
  즉 **사람이 판정해야 할 것이 많은 곳**에 붙습니다. 산출은 `docs/reports/<종류>-<날짜>.html`(형상 관리 제외).
  자기완결 HTML이라 외부 CDN·폰트를 부르지 않고, 라이트/다크를 따릅니다.
  **HTML은 정본이 아닙니다** — 정본은 md이고 리포트는 매번 다시 생성됩니다. 손으로 고치지 않습니다.
  강제 장치(`check-consistency.sh`)는 순수 bash 그대로라 **python3이 없어도 키트는 막을 것을 막습니다.**
- **부작용 지표** — S1 ④와 `/plan`에 추가. 주지표만 적어 두면 그것만 밀어붙이게 되고 대가는 아무도 안 보는 곳에서 치릅니다.
  *"응답 2초 → 0.5초"*만 있으면 정확도를 깎아서 달성해도 성공으로 보입니다. Lite는 면제합니다 — 나만 쓰는 것은 내가 나빠진 걸 바로 압니다.

### 변경

- **ID는 발급하지 않고 인용합니다 ★** — 키트는 ID 형식을 강요하지 않습니다.
  상류가 붙였으면 `FR-1`이든 `R-AOTSCK`든 `REQ-042`든 **그대로 옮깁니다.** 번호를 다시 매기면 같은 것에 이름이 둘이 되고, 그때부터 어느 쪽을 인용할지 매번 갈립니다.
  ID가 없는 것(화면 등)만 키트가 부여합니다.
  검사기도 고정 정규식을 버리고 **매핑표에 실제로 쓰인 ID에서 접두사를 유도**해 대조합니다.
- **계약 확인은 단정이 아니라 확인입니다 ★** — 이전 설계는 "상류는 권한 표·데이터 모델을 구조적으로 담지 않는다"를 **전제**했습니다. 틀렸습니다.
  기능마다 권한·규칙·데이터 정의를 채워 주는 계획 도구가 실제로 있습니다. 그걸 모른 채 저장소에 또 쓰면 **같은 것이 두 벌**이 되고, 상류가 바뀌는 순간 갈라집니다 — 갈라짐을 막으려고 만든 표가 갈라짐을 만드는 셈입니다.
  그래서 `/adopt`가 항목마다 실제로 뒤져 `상류 보유 / 갭 / 해당 없음`을 판정하고, **그 판정이 정본 소유권도 같이 정합니다.**
  표의 6항목은 **"반드시 확인할 최소 후보"**일 뿐이고, 후보 밖의 빈 곳을 발견하면 줄을 더합니다.
  다만 **상태 전이표는 거의 항상 갭입니다** — 어느 계획 도구도 데이터 상태 전이를 구조로 담지 않습니다.
- **검증 조건 개수만큼 테스트를 요구합니다 ★** — 매핑표에 `조건 수` 칸이 생겼습니다.
  요구사항 하나에 검증 조건이 5개인데 테스트가 1개면 나머지 4개는 **검증되지 않은 채 완료**가 됩니다. "테스트 칸이 비었나"만 보면 이걸 못 잡습니다.
  조건을 셀 수 없는 상류면 `—`로 두고 기존 규칙으로 내려갑니다 — 검사가 약해지는 것이지 꺼지는 게 아닙니다.
- **매핑표의 한 행 = 최상위 요구사항 하나.** 그 아래 기능·사양으로 쪼개져 있어도 행을 늘리지 않습니다 — 행의 단위는 **검증 조건이 붙어 있는 계층**입니다.
  기능 단위 진척은 사이클·Story가 맡습니다. 매핑표는 정합성 검사용이지 진척 관리용이 아닙니다.
- **인터뷰가 "묶어 묻기"로 바뀌었습니다.** 이전에는 "한 번에 하나씩"이었습니다.
  ① 지금 답할 수 있는 질문을 한꺼번에 ② 질문마다 추천안과 한 줄 근거 ③ 앞선 답이 정해져야 뜻이 생기는 질문은 다음 차례로 ④ 사실은 에이전트가 찾고 결정만 사용자에게.
  "잘 모르겠다"가 나오면 추천안을 안 붙였다는 뜻입니다. 정본은 `docs/guides/index.md` 한 곳이고 S1~S4가 참조합니다.
  **S4 2부(기술 스택)는 예외입니다** — 기술 결정은 서로 얽혀 있어 지금 답할 수 있는 질문 자체가 적고, 사용자가 배우면서 결정해야 하므로 깊이가 왕복 수보다 중요합니다.
- **`/review`가 2단계가 됐습니다.** 1단계 기계 검사(확정적) → 2단계 `code-review` 서브에이전트(판단).
  1단계가 실패하면 2단계로 넘어가지 않습니다 — 문서가 어긋난 상태의 diff는 기준 없이 읽는 것입니다.
  기계가 이미 확정한 것(고아 인용·커버리지·참조)을 서브에이전트에게 다시 판단시키지 않습니다.
- **`code-review` 서브에이전트에 검사 8번(요구사항 근거) 추가** — 이번 변경이 어느 요구사항을 구현하는지, 그 행이 가리키는 상태 전이·화면과 실제 구현이 일치하는지. 일반 코드리뷰가 못 하는 것이고, 프로젝트 스펙을 소유한 쪽만 할 수 있는 지적입니다.
- **`code-review`가 4축 16검사로 넓어졌습니다 ★** — 이전 8검사는 "규칙을 어겼나"에 치우쳐 있어서,
  **정상 경로 테스트가 전부 초록인 채로 지나가는 것**을 못 잡았습니다. 축을 넷으로 나눴습니다:
  **A 규칙 위반**(우회·드리프트·스택·범위) · **B 보안**(신뢰 경계·주입·남의 것 접근·노출) ·
  **C 의도치 않은 동작**(되돌릴 수 없는 행동·중복 실행·실패 모드·경합·한도) · **D 검증**(권한 양쪽·테스트 실질성·요구사항 근거).
  **기준을 밖에서 가져오지 않습니다** — 이 프로젝트가 무엇을 대비하기로 했는지는 `architecture.md` 3절 안정성 7종·4절
  (서버측 입력 재검증·중요 값의 서버측 재계산)과 `domain.md` 4절(남의 것 조회·되돌릴 수 없는 행동)·5절(어디서 강제하나)이
  **이미 적어 두었고**, 리뷰는 적어 둔 대비가 코드에 실제로 있는지를 봅니다. 거기 없는 위험을 발견하면 그것도 지적입니다(스펙의 구멍).
  대신 노이즈를 막는 규칙을 함께 넣었습니다 — 적용되지 않는 축은 `해당 없음` 한 줄로 닫고,
  **입력에서 도달점까지의 경로를 짚지 못하면 심각도를 낮춰 "확인 필요"로** 내립니다. 목록을 길게 만드는 것이 목적이 아닙니다.
- **리뷰 발견은 심각도로 거르지 않고 전부 기록합니다 ★** — `/review`가 "치명·높음만 기록"이라고 범위를 좁혀 두어
  `docs/quality/index.md` 원칙 1(기록 없이 고치지 않는다)과 어긋나 있었습니다.
  기록되지 않은 지적은 **2회차 승격 카운터에 잡히지 않아** 규칙으로 올라가지 못합니다 — 거른 만큼 학습 루프가 조용히 얇아집니다.
  "확인 필요"로 온 것도 `상태: 보류`로 기록하고, 판정한 뒤 열거나 닫습니다.
- **이슈 유형에 `보안`을 더했습니다** (`security/`) — 주입·입력 검증 누락·신뢰 경계 위반·민감정보 노출.
  기존 `권한`은 인가 판단(권한 없는 접근 허용)으로 두었습니다. 유형이 없으면 승격 카운터가 세지 못하고, 세지 못하면 규칙이 되지 않습니다.
- **S1~S5 가이드의 성격 전환.** 삭제하지 않고 각 가이드 머리에 "S0로 들어왔으면 갭이 가리키는 절만 편다"를 명시했습니다.
  다만 **무엇이 갭인지 단정하지 않습니다** — 실제로 읽어 보고 비어 있는 것만 폅니다.
- **유저플로우 ≠ 상태 전이표를 명시.** 화면 이동(A → B)과 데이터 상태 전이(접수 → 진행중)는 다른 물건입니다.
  전이표가 없으면 "결제 화면에서 뒤로 가면 주문 데이터는 무슨 상태가 되나"에 답이 없고, 답이 없으면 에이전트는 멈추지 않고 지어냅니다. 일탈의 진원지라서 S0가 이걸 별도 갭으로 잡습니다.
- **사이클 문서에 요구사항 ID 인용 필수.** `C00-template.md`의 "이번에 만들 것"이 표가 되고, 시작 전 확인에 `/adopt --sync`·정합성 검사·**상태 전이 칸이 `—`가 아닐 것**이 추가됐습니다.
- **계약 확인 판정 → 소유권 결정 설명을 `source-map.md` 4절 한 곳으로 모았습니다** — 같은 판정 표가
  `source-map.md`·`adopt.md`·`S0-adopt.md` 세 곳에 있었습니다. 두 벌을 금지하는 장치의 설명이 자신은 세 벌이었던 셈입니다.
  이제 판정 어휘와 소유권 귀속의 정본은 4절 하나이고, 커맨드와 가이드는 거기를 가리키기만 합니다.
- `install-kit.sh` — `.claude/scripts/` 복사·실행권한, `KIT_SEED`(없을 때만 넣는 프로젝트 소유 양식) 추가. 0.3.0에서 올라오는 저장소에도 새 파일이 들어가되 기존 내용은 건드리지 않습니다.
  그리고 설치·업그레이드가 끝난 뒤 `.claude/settings.json.dev-kit`이 남아 있으면 **"훅이 아직 등록되지 않았다"를 마지막에 크게 출력합니다** —
  기존 `.claude/`가 있는 저장소에서 훅만 조용히 빠지고 커맨드·가이드는 멀쩡히 돌아, 방법론이 산문 규칙만 남은 채 굴러가는 단일 실패 지점이었습니다.
- `scripts/check-docs.sh` — `.claude/scripts/*.sh` 실행권한·문법 검사, `.tsv` 참조, `upstream/` 경로 관례 추가.

### 검증

- 정합성 검사를 실측했습니다 — 도입 전 안내 → 통과 → 고의로 어긋뜨림(조건 수 미달·완료인데 테스트 없음·재검토 잔존·고아 인용·화면 불일치 **5종 전부 적발**, exit 1) → 조건 수가 숫자가 아닐 때 경고 → 수정 후 통과(exit 0).
  git 저장소 경로와 git 없는 경로를 따로 확인했고, 미커밋 파일도 검사 대상에 들어가는 것을 확인했습니다.
- `ADR-001` 같은 다른 ID 체계를 요구사항 ID로 오탐하지 않는 것을 확인했습니다 (`git grep`의 ERE는 `\b`를 지원하지 않아 앞 글자 포함 매칭 후 정확 일치로 거릅니다).
- **BSD awk가 한글 열 이름 비교를 어긋나게 하는 것을 발견해** 표 처리에서 awk를 걷어내고 순수 bash로 바꿨습니다 (`사이클`이 `출처`로 잡혔습니다). 열 위치를 고정하지 않고 머리행 이름으로 찾으므로 열이 늘어도 깨지지 않습니다.
- 가이드·양식 파일의 예시 ID를 고아 인용으로 오탐하던 것을 제거했습니다 — 인용을 검사할 곳은 "무엇을 만들고 있는가"를 적는 곳(사이클·Story·이슈·ADR·코드)입니다.
- 업그레이드 3경로 검증 — 신규 설치, 0.3.0 상태에서 업그레이드(새 파일 생성 + 프로젝트 소유 파일 보존), 재업그레이드(멱등).
- 테스트 실재 검사를 실측했습니다 — 유령 이름 적발(exit 1) → 실물 작성 후 통과, 사이클 문서에 적힌 이름은 실재로 오인하지 않음. git 경로·비-git 경로, macOS 기본 bash 3.2와 최신 bash 각각 확인.
- 그 과정에서 **빈 테스트 칸이 macOS 기본 bash 3.2에서 "완료인데 테스트 없음" 검사를 조용히 통과시키는 잠복 버그를 발견해 고쳤습니다** —
  `set -u`에서 빈 배열 확장이 `$()` 서브셸만 죽여 개수가 숫자 대신 빈 문자열이 됐습니다. 검사가 죽는 게 아니라 그 판정 하나만 조용히 빠지는, 가장 나쁜 형태였습니다.
- **폴백(비-git) 경로가 설치된 의존성 안에서 이름을 찾아 통과시키던 구멍을 실측해 막았습니다** — `node_modules` 안에만 있는 유령 테스트 이름으로 `정합성 검사 통과`가 나오는 것을 재현한 뒤(RED), 제외 목록을 더해 실패로 바뀌는 것(GREEN)과 실물 테스트를 쓰면 통과하는 것을 확인했습니다. git 경로도 `.gitignore` 된 디렉터리를 같은 방식으로 무시하는지 함께 확인했습니다.
- 훅 미등록 경고를 3경로로 실측했습니다 — 깨끗한 설치(경고 없음) · 기존 `.claude` 병합 설치(경고) · `.dev-kit` 잔존 상태의 업그레이드(경고).

## 0.3.0 — 2026-08-26

0.2.0 저장소 진단(42건)의 수정판입니다. 방향은 하나입니다 —
**안내가 정확하면 문서 정합성은 구조가 지키고, 사람과 에이전트는 앞으로 나아가는 데만 쓴다.**
서브에이전트가 리뷰·학습을 맡고, 정본이 하나씩 정해지고, 참조가 검증 가능해졌습니다.

### 추가

- **`code-review` 서브에이전트 + `/review` 커맨드** — 절대 규칙 11(검증 우회 금지)이 처음으로 산문 밖으로 나왔습니다. 구현한 컨텍스트는 자기 우회를 보지 못하므로, 리뷰는 별도 컨텍스트가 `code-conventions.md` 5-1 표를 기준으로 diff를 검토합니다. 구현 한 덩어리마다, 그리고 사이클을 닫기 전(`/cycle-close` 2단계)에 실행합니다.
- **절대 규칙 12 (비밀값)** — 비밀값 훅이 인용하던 규칙이 실제로는 존재하지 않았습니다(규칙 5는 에러 기록). 규칙을 신설하고 CHANGELOG·AGENTS·CLAUDE·훅·`.claude/README.md`의 매핑 5곳을 정정했습니다.
- **버전 스탬프와 설치/업그레이드 분리** — `CLAUDE.md` 첫 줄에 `<!-- dev-kit v0.3.0 -->`. 키트 소유 파일과 프로젝트 소유 파일을 표로 구분하고, `cp -R` 재복사가 프로젝트 증거를 파괴하는 경로를 업그레이드 절차로 대체했습니다. 기존 `.claude/settings.json` 병합 안내 포함.
- **`scripts/install-kit.sh`** — 설치·병합·업그레이드 자동화. 기존 `.claude/settings.json`은 덮지 않고 `.dev-kit` 사본으로 두며, 업그레이드는 소유권 표의 키트 소유 파일만 교체하고 `CLAUDE.md`는 `.dev-kit-new`로 두어 수동 병합한다.
- **저장소 자신의 바닥선** — 루트 `STATUS.md`·이슈 로그(로컬 전용, 형상 관리 제외)·문서 참조 검사 스크립트(`scripts/check-docs.sh`)와 CI. "Lite에서도 줄이지 않는 것"을 이 저장소가 스스로 지키지 않던 것을 바로잡았습니다. 외부에 공개하는 변경 기록은 이 CHANGELOG 하나입니다.
- **MIT 라이선스** — 공개 저장소인데 제3자가 사용·복제·수정할 법적 근거가 없던 것을 해소했습니다. 키트는 복사해 쓰라고 만든 것이므로 가장 제약이 적은 라이선스를 골랐습니다.

### 변경

- **프로파일이 DoD 안으로 들어갔습니다.** 절대 규칙 2가 DoD를 유일한 게이트로 선언한 이상, 축약도 DoD에 있어야 작동합니다. S1~S6 모든 DoD 항목에 `(Standard+)`·`(Full)`·`(사람 확인)` 표식을 달고, S2·S3·S4·S5·S6의 **입력**에 `profiles.md`를 추가했습니다. S4 DoD의 자기모순(프로파일 범위 vs 7종 전부)과 profiles.md의 S1 종료 조건 재정의를 제거했습니다.
- **사분면 질문을 2값으로.** "사내망"이 어느 열에도 매핑되지 않던 것을 "내 컴퓨터 밖이면 전부 원격"으로 정리했습니다 (사내망 → 원격 → Standard).
- **Epic 계층 제거.** 17곳에서 요구되고 0곳에서 정의되던 층입니다. 계획 계층은 마일스톤 → 사이클 → Story 셋으로 고정했습니다 (규칙 8: 과설계를 잘라낸다).
- **정본 지정.** 이슈 유형 표(`docs/quality/index.md` — 한글 유형 + 영문 폴더 슬러그), 금지된 우회 표(`code-conventions.md` 5-1 — S6·CLAUDE.md는 참조만), 필수 검수 시나리오(`test-scenarios.md` 13종 — S6 프롬프트는 참조만), 이슈 레코드 필드(`issues.md` 양식), stack 결정 표 5열. 같은 규범의 복사본이 세 판본까지 갈라져 있던 것을 하나로 접었습니다.
- **누적 파일에 STATUS.md의 규율 복제.** issues(검증됨 → archive, `/cycle-close` 6단계) · learning-log(200줄 → archive) · test-scenarios(자동화된 상세 절 제거) · roadmap 백로그(상한 30건)에 각각 상한·이동처·실행 주체를 명시했습니다. archive 하위 폴더는 "첫 항목 때 만든다"로 통일.
- **훅 3개 실질 보강.**
  - `guard-secrets`: `git commit -a/-am`(diff HEAD 스캔)·`git -C`·절대경로·sudo 접두어 탐지, GitHub fine-grained PAT·GitLab PAT·Stripe live·JWT·DB 접속 문자열 패턴 추가, `.env.sample/.template`·공개 인증서·lockfile 해시·테스트 픽스처 오탐 제거, **Write/Edit 경로 감시**(gitignore된 파일은 통과).
  - `guard-dependency`: stack.md 전체 부분 문자열 → **결정 표 "선택" 열 앵커 매칭**(react → react-dom 허용, 탈락 대체제 차단), cargo/go install·brew·pipx·uv pip·python -m pip 추가, sudo/환경변수 접두어 제거, `set -f` 글롭 보호, **매니페스트(package.json 등) 직접 편집 감시**.
  - `status-updated`: 한국어 표기 등 날짜 형식 5종 인정, 파일 기반 루프 차단기(30분 내 재차단 없음).
  - 공통: `jq` 부재 시 침묵 통과 → stderr 경고 후 통과.
- **어휘 분리.** 작업 상태 5종(⬜🔵🟡✅⛔)과 문서 상태(미작성/작성 중/확정)를 분리하고, 객관식·불리언 자리의 ⬜ 오용을 제거했습니다.
- 라우팅 표의 역할 분리 위반 수정 — 학습 산출물(`learning-log`·`rules-learned`)은 Main이 아니라 `/ingest-errors`가 쓰는 것으로. `rules-learned` 10개 초과 시 이관처를 guides(불변 계층)에서 `CLAUDE.md` 6절·`code-conventions.md`로 변경.
- 설치 절차에서 `chmod` 단계 제거 (훅 755 커밋), `/stage`의 인자 치환을 `$ARGUMENTS`로 수정.

### 수정

- **훅 실측 검증 4건** — 훅에 실제 PreToolUse 입력을 넣어 발견, 즉시 수정·재검증 —
  ① `git add X && git commit` 단일 명령이 비밀값 커밋 검사를 지나치던 구멍(훅 시점엔 스테이징이 비어 있다): 같은 명령의 add 대상(`-A`·`.`·디렉터리 포함)을 작업 트리에서 직접 검사.
  ② MultiEdit `edits[].new_string`을 두 guard 훅이 읽지 않아 내용 검사가 침묵 통과하던 것.
  ③ `pip install -r requirements.txt`를 패키지 도입으로 오인해 차단하던 오탐 — 값을 갖는 플래그의 인자를 건너뛴다.
  ④ 설치 안내의 "애드온 줄 주석 해제"가 실물(라우팅 표의 일반 행)과 어긋나던 서술 정정.
- 규범 표 열 수 통일 (S2 전이표 6열, S3 화면 목표 4열, C00 자동 검증 6행, stack 결정 표 5열 표기 4곳).
- ADR 템플릿의 미리 채워진 결론("A를 채택한다") 제거, 양식 표시·명명 규칙 추가. ST-000 권한 표의 예시가 답 칸에 들어 있던 것을 분리.
- 학습 승격(2회)과 프로파일 승급(3회) 임계의 관계 명시, Full에서의 "한 단계 위" 정의(설계·ADR 재검토).
- `learning-log.md`에 읽는 시점 부여 (쓰기 전용 → 인제스트 전 중복 확인), 카탈로그 누락 보완(각 폴더 index.md 등재).
- 경로 관례를 프로젝트 루트 기준 `docs/…` 하나로 통일. CHANGELOG 0.2.0의 규칙 매핑·경로 오류 정정.

## 0.2.0 — 2026-08-26

첫 자체 평가에서 드러난 구조적 결함과 미이행 계약을 정리했습니다. 방향은 하나입니다 —
**설계 단계는 사람이 놓치는 것을 잡고 있었지만, 구현 단계가 여전히 사람 손에 다 맡겨져 있었습니다.**

### 추가

- **검증 우회 금지 (절대 규칙 11)** — 실패하는 테스트 삭제·단언 완화·타입 무시·예외 삼킴·린트 비활성화로 통과시키는 것을 금지. AI에게 가장 흔한 실패 모드인데 이전 판에는 없었습니다. `code-conventions.md`에 스택별로 구체화할 표를 함께 추가했습니다.
- **`.claude/` 기계적 강제 계층** — 산문 규칙은 세션이 길어지면 드리프트합니다. 훅 3개가 실제로 막습니다: `stack.md`에 없는 패키지 설치(규칙 4), 비밀값 커밋(0.3.0에서 규칙 12로 정식화), STATUS 미갱신 세션 종료(규칙 9). `error-learning` 서브에이전트와 `/stage`·`/cycle-close`·`/ingest-errors` 커맨드도 함께 배포합니다.
- **`templates/dev-kit/docs/guides/profiles.md`** — S1의 난이도 사분면이 절차량을 정하게 했습니다(Lite/Standard/Full). 이전에는 사분면을 계산만 하고 쓰지 않아, 개인 도구에도 전체 절차를 요구했습니다. Lite에서도 비목표·상태 전이표·`stack.md`·자동 테스트·기록과 규칙 승격·STATUS 갱신·검증 우회 금지는 줄이지 않습니다.
- **`docs/plan/stories/ST-000-template.md`** — README와 CHANGELOG가 약속했으나 실물이 없던 Story 계약. 영향 범위(손대는 파일), **서비스 페르소나 권한과 개발 에이전트 작업 권한의 분리**, 허용·거부 행동, 검증 방법을 담습니다.
- **S6에 TDD 절과 자동 테스트 승격 절** — RED → GREEN → REFACTOR가 실제 구현 가이드에 들어왔습니다. 그리고 **버그를 잡아낸 검수 시나리오는 자동 테스트로 승격합니다.** 이게 없으면 수동 검수는 사이클이 늘수록 조용히 죽습니다.
- **스펙 드리프트 대조** — 사이클 종료 시 `spec/`과 구현을 대조합니다. "코드가 스펙을 앞서가지 않는다"는 규칙만 있고 확인 절차가 없으면 서너 사이클 뒤에 `spec/`은 읽으면 해로운 문서가 됩니다.

### 변경

- 폴더별 `index.md` / `MOC.md` 이중 구조를 **폴더당 한 파일로 통합**했습니다(최상위 `docs/`만 둘로 유지). 비어 있는 archive 폴더에까지 탐색 허브를 두는 것은 이 키트 자신의 과설계였습니다.
- 수동 검수 횟수를 프로파일 기준으로 바꿨습니다 (Full 15~20 / Standard 5~10 / Lite 1). 이전에는 모든 프로젝트에 15~20회를 요구했습니다.
- "높은 추론 설정"을 도구별 실제 설정으로 구체화했습니다 (Claude Code `ultrathink`, Codex reasoning effort `high` 이상).
- 사이클·이슈·검수 문서에 자동 테스트 승격 여부와 품질 검사 결과 칸을 넣었습니다.
- `docs/guides/index.md`에서 `decision-modes`·`error-learning-ingest`를 애드온이 아닌 **전 단계 공통**으로 재분류했습니다.

### 수정

- `docs/status/index.md`의 표 구분자 열 수 불일치, `error-learning-ingest.md`의 오타, `learning-log.md`와 가이드 사이의 양식 불일치.
- 파일럿 기록 위치를 명확히 했습니다 — 증거는 제품 저장소, 회고만 이 저장소.

## 0.1.0 — 2026-08-25

- 6단계 설계, 학습/실행 모드, Story 권한·영향 계약, Main/Subagent 역할 분리, 상태·archive 구조, 오류 학습 인제스트를 갖춘 `dev-kit`을 초기 배포했습니다.
- S4 산출물로 프로젝트별 `code-conventions.md`를 추가했습니다.

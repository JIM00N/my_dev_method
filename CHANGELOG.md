# Changelog

이 파일은 방법론·템플릿·가이드의 변경 이유와 적용 범위를 기록합니다. 제품별 기능 변경은 각 제품 저장소가 기록합니다.

## 0.2.0 — 2026-08-26

첫 자체 평가에서 드러난 구조적 결함과 미이행 계약을 정리했습니다. 방향은 하나입니다 —
**설계 단계는 사람이 놓치는 것을 잡고 있었지만, 구현 단계가 여전히 사람 손에 다 맡겨져 있었습니다.**

### 추가

- **검증 우회 금지 (절대 규칙 11)** — 실패하는 테스트 삭제·단언 완화·타입 무시·예외 삼킴·린트 비활성화로 통과시키는 것을 금지. AI에게 가장 흔한 실패 모드인데 이전 판에는 없었습니다. `code-conventions.md`에 스택별로 구체화할 표를 함께 추가했습니다.
- **`.claude/` 기계적 강제 계층** — 산문 규칙은 세션이 길어지면 드리프트합니다. 훅 3개가 절대 규칙 4·5·9를 실제로 막습니다: `stack.md`에 없는 패키지 설치, 비밀값 커밋, STATUS 미갱신 세션 종료. `error-learning` 서브에이전트와 `/stage`·`/cycle-close`·`/ingest-errors` 커맨드도 함께 배포합니다.
- **`guides/profiles.md`** — S1의 난이도 사분면이 절차량을 정하게 했습니다(Lite/Standard/Full). 이전에는 사분면을 계산만 하고 쓰지 않아, 개인 도구에도 전체 절차를 요구했습니다. Lite에서도 비목표·상태 전이표·`stack.md`·자동 테스트·기록과 규칙 승격·STATUS 갱신·검증 우회 금지는 줄이지 않습니다.
- **`plan/stories/ST-000-template.md`** — README와 CHANGELOG가 약속했으나 실물이 없던 Story 계약. 영향 범위(손대는 파일), **서비스 페르소나 권한과 개발 에이전트 작업 권한의 분리**, 허용·거부 행동, 검증 방법을 담습니다.
- **S6에 TDD 절과 자동 테스트 승격 절** — RED → GREEN → REFACTOR가 실제 구현 가이드에 들어왔습니다. 그리고 **버그를 잡아낸 검수 시나리오는 자동 테스트로 승격합니다.** 이게 없으면 수동 검수는 사이클이 늘수록 조용히 죽습니다.
- **스펙 드리프트 대조** — 사이클 종료 시 `spec/`과 구현을 대조합니다. "코드가 스펙을 앞서가지 않는다"는 규칙만 있고 확인 절차가 없으면 서너 사이클 뒤에 `spec/`은 읽으면 해로운 문서가 됩니다.

### 변경

- 폴더별 `index.md` / `MOC.md` 이중 구조를 **폴더당 한 파일로 통합**했습니다(최상위 `docs/`만 둘로 유지). 비어 있는 archive 폴더에까지 탐색 허브를 두는 것은 이 키트 자신의 과설계였습니다.
- 수동 검수 횟수를 프로파일 기준으로 바꿨습니다 (Full 15~20 / Standard 5~10 / Lite 1). 이전에는 모든 프로젝트에 15~20회를 요구했습니다.
- "높은 추론 설정"을 도구별 실제 설정으로 구체화했습니다 (Claude Code `ultrathink`, Codex reasoning effort `high` 이상).
- 사이클·이슈·검수 문서에 자동 테스트 승격 여부와 품질 검사 결과 칸을 넣었습니다.
- `guides/index.md`에서 `decision-modes`·`error-learning-ingest`를 애드온이 아닌 **전 단계 공통**으로 재분류했습니다.

### 수정

- `status/index.md`의 표 구분자 열 수 불일치, `error-learning-ingest.md`의 오타, `learning-log.md`와 가이드 사이의 양식 불일치.
- 파일럿 기록 위치를 명확히 했습니다 — 증거는 제품 저장소, 회고만 이 저장소.

## 0.1.0 — 2026-08-25

- 6단계 설계, 학습/실행 모드, Story 권한·영향 계약, Main/Subagent 역할 분리, 상태·archive 구조, 오류 학습 인제스트를 갖춘 `dev-kit`을 초기 배포했습니다.
- S4 산출물로 프로젝트별 `code-conventions.md`를 추가했습니다.

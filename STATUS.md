# STATUS — my_dev_method 저장소 상태

> 이 파일은 방법론 저장소 **자신의** 상태다. 키트가 제품 저장소에 요구하는 것의 최소형(Lite)을 스스로 지킨다.
> 제품 프로젝트의 STATUS는 각 제품 저장소의 `docs/status/STATUS.md`가 소유한다.

**최종 갱신**: 2026-08-27
**프로파일**: Lite (문서 저장소 — 나만 쓰고, 로컬에서만)
**현재 버전**: 0.7.0 (작업 중 — 커맨드·서브에이전트 이름에 `mdm-` 접두를 붙인 판)

## 커맨드·서브에이전트 `mdm-` 개명 (2026-08-27, 사용자 지시 — 커밋 승인 대기)

사용자 지시("모든 스킬들 이름 앞에 mdm 붙이자"). 범위는 **키트 커맨드 7 + 이 저장소 `/kit-review` +
서브에이전트 전부**, 표기는 `mdm-` **파일명 접두**다(`mdm/` 하위 디렉토리 네임스페이스가 아니다 — 사용자 선택).
`git mv` 17개 + 참조 **482자리** 일괄 수정.

- 키트: `/adopt`·`/plan`·`/ready`·`/review`·`/stage`·`/cycle-close`·`/ingest-errors` → `/mdm-*`,
  `code-review`·`error-learning` → `mdm-*` (frontmatter `name:` 포함)
- 이 저장소: `/kit-review` → `/mdm-kit-review`, `kit-review-k1`~`k6`·`kit-refute` → `mdm-*`
- `scripts/check-docs.py` 검사 7 의 하드코딩(`kit-review-k(\d+)`·`startswith`)과
  `scripts/test-docs-check.sh` fixture 문자열도 같이 갱신
- **CHANGELOG 0.6.0 이하 항목의 산문은 안 바꿨다** — 그때의 이름이 그때의 사실이다.
  깨지는 백틱 경로 1자리만 고쳤다(검사 1-c 대상). `issues.md`·`handoff-0.5.0.md` 는 산문까지 바꿨다(작업 문서라서).

**개명이 스스로 낳은 결함 — 두 판을 만들었다가 결국 뺐다 (2026-08-27, 사용자 결정).**
`merge_claude` 는 새 이름을 넣을 뿐 옛 이름을 건드리지 않아, 업그레이드하면 `/adopt` 와 `/mdm-adopt` 가
**한 저장소에 둘 다 살아 있게** 된다. 이것을 설치기가 자동으로 처리하게 만들었고 **두 판 다 치명을 받았다**:

- **1판 `rm -f`** — 버전·소유권 확인이 없어 v0.7.0 → v0.7.0 재업그레이드에서도 돌았다. 반증이 근거를
  더 세게 찾았다: 설치기가 신규 설치에서 **화면에 인쇄하는** 「그 저장소 자신의 것이면 그대로 둬도 된다」가
  다음 업그레이드에서 **거짓이 된다.**
- **2판 개칭(`*.dev-kit-0.6-retired`) + 가드 넷** — 2회전 K2 가 결정타를 놨다. 버전 가드가 `CLAUDE.md`
  스탬프에 의존하는데 **업그레이드는 그 파일을 갱신하지 않는다**(`.dev-kit-new` 만 쓴다). `CUR_VER` 은
  영구히 0.6.0 이고 가드는 **실제 경로에서 한 번도 닫히지 않았다.** 그때 내 fixture 는 손으로 스탬프를 올려
  **실제 경로가 만들 수 없는 상태**를 재고 있었다 — 검사가 검사인 척한 자리다.
  `mv` 가 지난 물림 사본을 조용히 덮는 것도 K2·병행 세션 K4 가 독립으로 잡았고 내가 직접 재현했다.

**뿌리는 하나였다: 설치기는 「이 파일이 키트 것인가」를 알 방법이 없다.**
사용자에게 물었고 **「이관 자동화를 뺀다」**를 골랐다. 지금 `install-kit.sh` 는 옛 이름을
**찾아서 알리기만 한다**(`warn_renamed`, 설치·업그레이드 양쪽 같은 문구). 처리는 키트 README
「업그레이드」 **4-1** 의 손 절차 — 파일을 열어 자기 것인지 아는 사람이 판단한다.
`retire_renamed`·`ver_before_070`·`path_has_symlink`·`RETIRED_*` 전부 삭제.

**함께 들어간 것 (이관 장치와 무관해서 유지)**
- `docs/plan/index.md`·`docs/decisions/index.md` 를 KIT_OWNED → KIT_SEED 로 강등
  (병행 세션 검사 I 의 확정 처방 — 그 표에 사이클 행·ADR 행이 실린다).
  **그 대가를 CHANGELOG 0.7.0 「양식 변경」 절로 실제로 갚았다** — 2회전 K5 가 「가리킨 통로가 비어 있다」를
  올렸고(반증 후 보통), 그 통로를 이번에 채웠다.
- 소유권 표 정정: `.claude/` 는 **키트가 만든 파일만** 키트 소유. `settings.json` 은 교체하지 않는 예외로 명시.
- `.githooks/*` 의 `/kit-review` → `/mdm-kit-review` (스윕이 **확장자 없는 파일**을 건너뛰었다) + `git am` 한계 명시.
- 업그레이드 안내 grep **세 줄** — 경로 오탐 제거(옛 판은 104~107건 전부 오탐) + 서브에이전트 이름 + 파일 경로 형태.
- **`check-docs.py` 검사 11 신설** — 키트 `.claude/{commands,agents}/**/*.md` 의 파일명·`name:` 이 전부
  `mdm-` 접두인지(**하위 디렉토리까지** — 2회전 K2 가 `commands/extra/deploy.md` 로 뚫었다).
  fixture 4케이스, `test-docs-check.sh` 30 → **34**.
- `scripts/test-install-upgrade.sh` — **불간섭을 재는 13 케이스 + 뮤테이션 1**. 0.6.0 합성이
  **본문의 커맨드 이름까지** 되돌려 「업그레이드가 키트 소유 문서를 갱신하는가」를 실질로 잰다
  (2회전 K4 의 「가짜 0.6.0 fixture」 지적 반영). 뮤테이션은 **뮤턴트의 rc·출력을 먼저 단언한다.**

**워크플로 아티팩트**(절대 규칙 4): 같은 트리에서 검사 I 를 작업한 병행 세션이 **개명까지 병합해 재배포했다**
(아래 §검사 I). 이 세션이 실물을 읽어 `mdm-` 표기가 반영된 것을 확인했다(옛 이름 잔존 0건).

### 1회전 리뷰 — 6축 중 5축 실패, 확정 치명 1 · 높음 3

`/mdm-kit-review` 6축. 1단계 기계 검사 **6종은 그때도 전부 통과했다** — 세 회전 연속으로
「1단계 통과 ≠ 완결」이 실증됐다. 발견 48건은 `issues.md` **#190~#237**(병행 세션은 #240부터).

**확정**: #190 치명(`drop_renamed` 가 버전·소유권 확인 없이 `rm -f`) · #191 높음(설치가 「그대로 둬도
된다」고 남긴 파일을 다음 업그레이드가 삭제) · #192 높음(키트 README 손 업그레이드 절차에 옛 이름
처리 단계 없음) · #202 높음(`check_list_sync` 가 한 방향만 봐서 목록이 **넓어지는** 것을 못 잼).

**반증이 6건을 강등시켰다.** 근거는 대부분 **「HEAD 에서도 같은 파일이 같은 강도로 파괴됐다」**
(선재 vs 증분 가르기)였다 — 심링크 탈출은 「한계 노출 0」으로 낮음까지 내려갔고, 비-키트 저장소
삭제는 순증분이 **거짓 사유 문구 하나**로 좁혀졌다. 반대로 치명 1건은 반증이 **더 강한 근거를 찾아**
유지시켰다. 이 축들이 없었으면 파괴적인 판이 그대로 커밋됐을 것이다.

### 2회전 — 이관 장치가 다시 잡혔고, 사용자 결정으로 장치를 뺐다

2회전(6축)에서 **K1 조건부 · K3 조건부 · K5 실패(높음 1) · K4 실패(높음 2) · K2 실패(치명 1·높음 1)**.
K6 은 사용자가 중단시켰다(장치를 빼기로 하면서 대상이 사라졌다).

**K2 의 치명이 결정적이었다** — 2회전에서 내가 넣은 **버전 가드가 실제 경로에서 한 번도 닫히지 않는다**는 것.
업그레이드는 대상 `CLAUDE.md` 를 갱신하지 않으므로 `CUR_VER` 이 영구히 0.6.0 이고, 내 fixture 는
손으로 스탬프를 올려 **실제 경로가 만들 수 없는 상태**를 재고 있었다.
K4 는 그 fixture 가 **가짜 0.6.0**(본문이 이미 새 이름)이라 실질을 못 잰다는 것을 따로 잡았다.
K5 높음(「양식 변경」 통로가 비어 있다)은 반증이 **보통으로 강등**했고(`docs/index.md` 가 등재 규칙을
완전한 형태로 나른다), 그럼에도 통로 자체는 이번에 채웠다.

**사용자에게 판단을 물었다** — 확정 결함이 전부 개명이 아니라 이관 장치에서 나오고, 두 판 연속 치명이며,
3회전이 상한이라는 것을 그대로 전달했다. 답: **「이관 자동화를 뺀다」.**
그 결정으로 위 치명·높음의 **대상 코드가 사라졌다** — 강제로 통과시킨 것이 아니라 기능을 뺀 것이다.

### 3회전 — **돌리지 못했다 (2026-08-28, 사용자 결정으로 중단)**

⛔ **이 트리는 3회전 재리뷰를 거치지 않았다.** 조용히 넘어가지 않고 여기 남긴다.

장치 제거 뒤 6축 3회전을 **두 번 띄웠고 12번 다 머신 절전으로 죽었다**(리뷰 결과가 아니라 인프라 실패).
사용자에게 그대로 알리고 판단을 물었다 — 「2축으로 좁힌다 / 여기서 멈추고 기록한다 / 6축 재시도」 중
**「여기서 멈추고 기록한다」**를 골랐다. 근거로 함께 올린 것: ① 12/12 실패라 조건이 그대로면 결과도 같다
② **1·2회전이 잡은 치명·높음은 전부 이관 자동화에서 나왔는데 그 코드가 삭제됐다** — 남은 것은 개명 스윕
(기계 검사가 덮는다) + 출력만 하는 `warn_renamed` + 목록 2줄 + 검사 11 + fixture 라, 6축 전면 리뷰는
**사라진 위험**에 맞춘 규모다 ③ 한 회전에 서브에이전트 토큰 약 70만.

**죽은 에이전트가 남긴 단서 셋은 전부 진짜 결함이었고, 에이전트 말을 믿는 대신 직접 재현해 고쳤다:**
- 3회전 K3 (중단 전 메모) — 0.6.0 합성 정규식의 lookbehind 가 `/` 를 배제해 **`/mdm-adopt`·
  `commands/mdm-adopt.md` 가 하나도 안 되돌려졌다**(133건 잔존). 「업그레이드가 키트 소유 문서를
  갈아 준다」 단언이 **공허하게 통과**하고 있었다. → `(?<![\w-])` 로 고치고 **fail-loud 상한**을 넣었다.
- 3회전 K3 (중단 전 메모) — `cksum $OLD_NAMES` 가 파일 하나가 없으면 그 실패를 `$(...)` 가 삼켜
  before/after 가 **둘 다 8줄**이 되어 「같다」로 통과한다(**9개 중 8개만 재면서 전부 초록**).
  → 몇 개를 쟀는지 세어 단언하게 고쳤다.
- 3회전 K5 (중단 전 메모) — **사라진 장치를 현재형으로 가리키는 문장 4곳**
  (루트 `CLAUDE.md` 자동 검증 · `mdm-kit-review.md` 1단계 · CI 스텝 라벨 · 키트 `.claude/README.md` 이름 규칙).
  → 전부 「설치기는 건드리지 않는다」로 고쳤다.

**지금 있는 증거 / 없는 증거 (정직하게)**
- 있다: 1단계 **6종 전부 통과** · `test-install-upgrade.sh` 를 **뮤테이션 3종**(guides 교체 제거 · 알림 제거 ·
  합성 파손)으로 돌려 각각 붉어지는 것을 실측 — fixture 가 공허하지 않다 ·
  사라진 장치를 가리키는 참조 **전수 grep 0건**.
- **없다: 최종 상태에 대한 독립적인 적대적 검토 한 번.** 위 셋은 전부 **내가** 확인한 것이고,
  이 저장소가 두 회전 연속 실증한 명제는 「**1단계 통과 ≠ 완결**」이며 「만든 컨텍스트는 자기 구멍을 못 본다」이다.
  그 한계를 안고 커밋하는 것이다.

선례: 이 파일 아래 §「2회전 재리뷰 유예 (사용자 결정)」 — 같은 처리다.

## 검사 I — 문서 등재 대조 (2026-08-27, 사용자 승인으로 착수 — 커밋 승인 대기)

아래 §index 등재 지시에서 「사용자 판단 대기」로 남겼던 **고아 등재 검사를 사용자가 승인**해("진행하자") 넣었다.

- `check-consistency.sh` **검사 I**: 사이클(cycles/+archive/cycles/ ↔ plan/index 현황 표, **양방향**) ·
  ADR(↔ decisions/index 목록 표, **양방향**) · Story(활성 문서→행 필수 + archive로 닫힌 문서의 잔존 행).
  **문서 없는 Story 행은 일부러 안 잡는다** — 프로파일에 따라 Story는 문서 없이 사이클 문서에만
  존재하는 것이 정상이라, 잡으면 설계상 오탐이 정상인 검사가 된다(Story 크기 검사를 안 만든 것과 같은 근거).
  표 파싱은 순수 bash(reg_table/reg_ids), 양식 파일(C00·ST-000·ADR-000)은 대상 제외. bash 3.2 실측 통과.
- `scripts/test-consistency.sh`에 **fixture 9케이스 + 뮤테이션 자기검증**(I 블록 제거 시 신호 소멸). CI는 기존 등록으로 함께 돈다.
- 등재 칸 형식을 정본 자리에 못박음: 사이클 현황 `#`=`C01` · ADR 목록 `#`=`ADR-<nnn>` · STATUS 활성 표 `ID`=`ST-<nnn>`.
  mdm-review 1단계 표에 실패 유형 2행 추가. 검사 목록 8종→**9종(A~I)** 갱신: 키트 README·키트 CLAUDE·docs/index·루트 CLAUDE·kit-review 1단계.
- **워크플로 아티팩트 갱신 완료** — 검사 9종 표에 I 행, 사슬 도면에 I 지점, 그리고 **mdm- 개명까지 병합 반영**(read→병합→재배포).
- **병행 세션 조율**: mdm- 개명 스윕(my-dev-method-2a)과 같은 트리에서 진행. 게이트가 부분 커밋을 막으므로
  **한 커밋으로 묶는다** — 그쪽의 잔여 작업(설치기 옛 파일 제거 + fixture · 버전 0.7.0 · CHANGELOG) 완료 후
  합쳐서 리뷰·도장·승인. 버전은 그쪽 제안대로 **0.7.0**.

### /mdm-kit-review 1회전 (6축) — 실패 → 수정 완료, 2회전 진행 중

**확정 치명 1 · 높음 4** (반증 5건 경유 — 상세는 `issues.md` #240~#264 표):
- **#240 치명 (K2)**: 매핑표 셀의 `<br>` 이 플레이스홀더 필터에 걸려 **「도입 전」 오판 + exit 0** —
  검사 전체(A~I) 소등 + 거짓 보고. --sync 재검토 나열이 유도하는 정상 입력이라 반증이 오히려 근거를 보강했다.
- **#241~243 높음 (K2)**: reg_table 이 첫 비-표 줄에서 끊겨 빈 줄 뒤·둘째 표의 유령/잔존 행이 무언 통과.
- **#244 높음 (K4, 열림)**: `--upgrade` 가 KIT_OWNED 인 plan/decisions index 를 통째 교체해 **등재 행 파괴**
  후 사용자 과실로 보고 — 동일 판 재업그레이드에서도 재현(구조 충돌). 처방 확정: 두 index 를
  **KIT_SEED(copy_if_absent)로 강등** — 구현·fixture 는 병행 세션, **사용자 승인 별도 항목**.
- K5 높음(종료 절차의 Story 행 삭제 누락)은 반증이 **보통으로 강등** — 3곳에 구절 추가로 닫음.

수정은 규칙대로 **fixture 먼저**: R1~R12 를 심어 **RED 12건 실측** 후 검사기 수정(플레이스홀더 필터를
「칸 전체 <…>」로, reg_table 절 전체 스캔+코드펜스+마지막 줄, reg_check fail-loud, norm_id 장식 제거,
역방향 C 접두 가드) → 전부 GREEN + archive 글롭 뮤테이션 추가. 1단계 5종·bash 3.2 통과.
**2회전**: K4 **조건부 — #244 수정 확인**(행 보존·retired 무오탐·수명주기 5경로). K2 **실패 — 높음 3 확정**
(#267 부분 placeholder 행 면제 · #268 앞공백 행 무음 · #269 같은 절단이 정본 매핑표에도) → fixture RED
10건 실측 후 수정(ID 칸 앵커 필터 · 앞공백 제거 · table_of 재작성 · reg_check 실패 승격 · norm_id 확장).

**3회전(상한) — K2 조건부 통과, 치명·높음 0. 검사 I 축 종결.** 수정 8/8 실측 유지, 신규는 오탐 방향
낮음 2건뿐(조용한 우회 0). 실물 출하 키트에서 도입 전 exit 0 · 정상 도입 오탐 0 · **위반 8유형 전부 검출**.
회귀 62/62 · 1단계 6종 · bash 3.2 통과.

**이번 커밋 범위 밖으로 남긴 것 (사용자 판단 대기)**: #276 `check-docs.sh` 검사 2 가 gitignore 문서까지
훑어 **로컬만 붉어지는** 어긋난 검사 — 같은 파일이 원칙으로 배제한 자리이고 이번 세션에 **두 번 실증**됐다.
#261 `--sync` 단독 경로 검사 재실행 · #262·#266 KIT_OWNED/SEED 의 다른 프로젝트-데이터 인스턴스 ·
#264·#272 CI 로케일(NBSP) 실측.

## index 등재·갱신 지시 (2026-08-27, 사용자 지시 — 커밋 승인 대기)

사용자 지적("문서 간 관계 표현·index 갱신이 제대로 쓰이는가") 점검 결과의 반영분.
키트에 **문서를 만들면 등재하라는 지시가 0건**이던 것을 닫았다 — templates/dev-kit 7개 md:
사이클(현황 표) · ADR(목록 표) · Story(STATUS 활성 병렬 작업 표) 등재 지시 + docs/index 일반 원칙 +
cycle-close 8·12단계 · S6 1절·8절 · C00 종료 점검 · 템플릿 `←` 줄. ~~기계 검사는 없다(의도적) —
고아 등재 검사 + fixture 도입 여부는 사용자 판단 대기~~ → **닫힘: 위 §검사 I로 도입됐다.**

`/mdm-kit-review` 1회전 (K1·K5) **통과** — 치명·높음 0, 발견 5건(#185~#189) 전부 같은 턴에 수정·닫힘.
1단계 5종 수정 전후 통과. 워크플로 아티팩트는 이번 입자에서 영향 없음 확인(갱신 불필요).

**함께 내린 판단 (사용자와 논의 중)**: 코드 간 관계 지도(요구사항→구현 파일 칸·별도 지도 문서)는
**만들지 않는 쪽으로 권고** — import가 정본인 것을 옮겨 적으면 두 벌이 되고, 기계는 경로 실재까지만
검증 가능해 조용한 드리프트가 남는다. 대안 후보(S6 8절 드리프트 대조에 architecture 1절·경계표 추가)는 미반영.

## 열린 이슈 백로그 A군 정리 + 1회전 리뷰 (2026-08-27)

열려 있던 **16건**(#001~#006 · #008 · #009~#012 · #032 · #077 · #089~#091) 중
「이미 있는 것의 결함」 **10건**을 처리했다. 남은 것은 **#001~#006 여섯 건 + #109(반쪽 트리, 높음)**
이고, 앞의 여섯은 「결여 영역」이다
(롤백·데이터 마이그레이션 · 보안 절차 · 접근성 · 관측성·성능 예산·릴리스 절차 · 멀티레포 · 협업·토큰 예산).
**이미 있는 것의 결함이 아니라 키트에 없는 절차 층**이라, 각각 프로파일별 배치와 강제 장치
(또는 명시적 「권고」 라벨)를 함께 설계해야 한다.

### 1회전 리뷰 — 6축 전부 실패, 확정 높음 7

`/mdm-kit-review` 6축을 돌려 **전부 실패**했다(높음 후보 12 → 반증 후 **확정 7 · 강등 4 · 범위 밖 1**).
1단계 기계 검사 4종은 그때도 전부 통과했다 — **통과가 완결의 증거가 아니라는 것을 다섯 축이 각각 실물로 보여줬다.**

확정된 것의 뿌리는 하나였다: **새로 넣은 검사들이 "추출 0건 = 통과" 구조인데, 붉어지는 증거가 저장소에 없었다.**

- 반증이 `CLAUDE.md`의 자백(*"검사기 자신이라 자기검증이 없다 — 손으로 뮤테이션을 돌린다"*)을
  **방어선으로 인정하지 않았다.** 그 자리에서 fixture 를 각 5~10줄로 만들어 보이며 **전제 자체를 반증**했고,
  그 정책이 **바로 이 변경분에서 실패했음이 실증**됐다(검사 셋이 무장해제 상태로 커밋 직전까지 왔다).
- **내 `#032` 수정은 순수 해악이었다** — `trap` 이 서브셸 밖에 걸려 아무것도 정리하지 않았고,
  `INT TERM HUP` 을 덧붙인 탓에 Ctrl-C 를 삼켜 **틀린 진단**까지 냈다(HEAD 는 rc 130, 내 판본은 rc 1).
- 검사 5 는 **로케일에 따라 통째로 무력해졌다** — 부정 문자클래스가 C 로케일에서 바이트 클래스가 된다.

### 고친 결과

- **`scripts/test-docs-check.sh` 신설**(CI) — `check-docs.sh` 의 **검사 1-c·5·6·7·8·9·10 에 위반을 심어**
  붉어지는지 실측한다. 처음 실행에서 **16개가 실패**했고 그것이 확정 높음 전부를 독립 재현했다. 지금은 **30/30**.
  **검사 1·1-b·2·3·4 는 아직 증거가 없다**(#159) — "각 분기"라고 쓰지 않는다.
  **exit code 만 보지 않고 기대 메시지까지 단언한다** — 다른 이유로 붉어진 것을 "잡았다"로 세면 hollow 해진다.
- **`scripts/check-docs.py` 분리** — 1-c·5·6·7·8·9 를 파이썬으로 옮기고 **검사 10(셸 함정 lint)** 을 더했다.
  로케일·멀티바이트에 흔들리지 않고, 추출이 0건이면 **실패**한다. python3 는 이제 필수다(없으면 실패).
- **`review-stamp.sh` trap 재수정** + 게이트 fixture 에 **시그널 케이스** 추가.
  트랩을 지워도, **1회전의 옛 위치로 되돌려도** 붉어지는 것을 확인했다.
- 검사 7 은 이제 **축 표를 실제로 파싱**해 오배선(K2↔K3 스왑)을 잡고, 검사 9 는 `tools:` 미지정·YAML 리스트·
  `Edit`·하위 디렉토리까지 대상으로 삼는다.

**0.6.0 푸시 후 CI 가 하나를 더 잡았다 (#153)** — 검사 8 fixture 가 실물 `issues.md` 에 append 하는 구조였는데
그 파일은 `.gitignore` 대상이라 CI 에는 없다. **검사는 옳았고 fixture 가 틀렸다.**
더 아픈 것은 `issues.md` #124 에 "합성 issues.md 로 CI 에서 돌린다"고 **적어 놓고 안 만든 것**이다 —
이 회차가 세운 규칙을 그 회차 안에서 어겼다. 지금은 합성 파일을 써 넣고 `git archive HEAD` 로 CI 조건에서 확인한다.

### 2회전 — 6축 중 4축 실패, 범위를 좁혀 진행 (사용자 결정)

2회전을 6축 전부에 돌려 **K1(높음 5)·K2(4)·K3(2)·K6(6) 실패, K4·K5 조건부**가 나왔다.
1단계 5종은 그때도 로컬·CI 조건 양쪽에서 전부 통과했다 — **통과가 완결의 증거가 아니라는 것이 두 회전 연속 실증됐다.**

가장 아픈 것은 **회귀 검사가 남의 파일을 지운 것**(#154)이다. 12-b 가 공용 `$TMPDIR` 을 통째로
스냅샷해 그 창에 남이 만든 `tmp.*` 를 누수로 오인하고 `rm -rf` 했다 — K3 이 이웃 프로세스를 세워
디렉토리 3개 삭제를 실측했고, **이 리뷰 세션의 5개 에이전트 동시 실행이 정확히 그 조건이었다.**
그리고 그 수정 자체를 K2 가 다시 깨뜨렸다: 전용 `TMPDIR` 로 가두는 방식은 macOS 에서
무템플릿 `mktemp -d` 가 `TMPDIR` 을 안 따라 **탐지가 죽는다.** 접두어(`kit-stamp.`)로 최종 해결했다.

**사용자 결정으로 범위를 좁혔다** — 파괴적인 것과 거짓 주장만 고치고 fixture 확대는 다음 회차로.
근거: 이 세션에서 **수정 회차마다 새 결함이 나왔고**(12-b 는 한 턴 안에서 파괴적 → hollow → 정상),
`mdm-kit-review.md` 상한이 3회전이다.

**고친 것 (11건)** — #154 파괴적 삭제 · #155 검사 5 자기라벨 폴백이 검사를 통째로 뚫던 것 ·
#156 awk lint 가 한 줄짜리만 보던 것 · #157 fixture 31케이스가 한 디렉토리를 공유하던 것 ·
#158 **"각 분기" 과대주장** 정정 · #172~#177 사본·숫자 정정.

**남긴 것 (19건, `issues.md` 2회전 표)** — 첫 항목은 **#159**: `check-docs.sh` 검사 1·1-b·2·3·4 의
RED 증거가 없다. 특히 **검사 3(훅 실행 권한)은 스스로 «유일한 그물»이라 적은 절대 규칙 1 의 fail-open 방어선**인데,
K1 이 «그 검사를 통째로 꺼도 30/30 초록»을 실증했다. **#103 이 한 층 위에서 재발한 것이다.**

**정정 기록**: 1회전이 #100 ② 를 "신호를 삼켜 계속 돈다"고 적었으나 이 스크립트에서는 관측되지 않는다
(2회전 실측: 두 판본 모두 rc 130). 그 차이를 재는 케이스를 만들었다가 **hollow 라 걷어냈다.**

## 마일스톤 층에 절차와 강제 장치 (2026-08-27)

마일스톤은 `roadmap.md`의 **빈 표**였다 — 누가 언제 긋는지, 무엇으로 나누는지 정한 곳이 없었다.
`/mdm-adopt` 7단계(정본 `S0-adopt.md` 7절)로 배치를 세우고, `/mdm-cycle-close` 9-1로 다음 마일스톤을 연다.
규율은 **행은 처음에 전부, 달성 조건은 M1만** — 나누는 신호 4개의 정본은 `docs/plan/index.md` 핵심 원칙 5.
장치: 매핑표 `마일스톤` 열 + **정합성 검사 H**(배치 빔=실패, 사이클 5바퀴 초과·요구사항 1개=경고)
+ `scripts/test-consistency.sh` 회귀 fixture(뮤테이션 자기검증 포함, CI 등록).
fixture 가 작성 중 실제 버그를 잡았다 — `$(... | sort -u)` 후행 개행 유실로 마지막 줄이 안 세어져 6바퀴가 5로 잡혔다.

**남은 것**: (a) 신호 4개 중 "배포 없이 끝난 사이클"은 archive 문서를 전부 읽어야 해서 검사를 안 만들었다 — 권고로 남김.
(b) ~~사이클 크기 신호가 `docs/plan/index.md` 4번과 `S6-build.md` 1절에 이중 기재~~ → **닫힘** (아래 §Story 크기, 이슈 #061).
(c) 범위 카운트 검사 5개(비목표·부작용 지표·백로그 30건·DoD 10개·화면 5개)는 여전히 체크박스뿐. 미착수.
(d) ~~키트 CLAUDE.md 절대 규칙 번호가 11 → 13 → 12 순으로 어긋나 있다~~ → **해소됨**:
   병행 세션이 규칙 13(커밋 정책)을 넣던 중간 상태를 본 것이고, 현재는 9→10→11→12→13 정상이다.

## Story 층에 크기 기준 (2026-08-27) — codex 외부 리뷰 채택

마일스톤에는 나누는 신호 4개, 사이클에는 3개가 있는데 **Story만 없었다.** 정의도 순환이었다
(*"한 에이전트가 끝까지 책임지는 가장 작은 단위"*). 정본은 **`docs/guides/ready.md` 「Story 크기 — 어디서 멈추나」** 한 곳이고,
`docs/plan/index.md` 원칙 6과 `S6-build.md` 1절은 가리키기만 한다.
**멈추는 조건**: 한 문장이 트리거 하나·종료 상태 하나로 써지고 정상·거부·오류를 함께 검증할 수 있으면 거기서 멈춘다.
`/mdm-ready`에 **2단계 크기 판정**을 넣었다 — 위치는 **초안 뒤·질문 앞**(트리거·결과 슬롯이 판정의 입력이라 순서를 못 바꾼다).

**기계 장치는 없고 표에 그렇게 적었다** (절대 규칙 3 후단). 제목의 "및/또는"을 세는 검사는 **일부러 안 만들었다** —
*"권한 확인 및 파일 선택"*이 정상 사례라 설계상 오탐이 정상인 검사가 되고, 무시하라고 만든 경고는 다른 경고까지 무디게 한다.

**리뷰 2회전 (K1·K5)** — 1회전 실패(K1 높음 1 · K5 높음 4) → `mdm-kit-refute` 4건 → **높음 확정 0**(전부 강등).
강등된 것까지 13건 전부 고치고 2회전을 돌려 **두 축 조건부 통과**(치명·높음 0). 2회전이 11건을 더 잡았다(#078~#088).
**2회전 지적의 대부분이 "사본 중 하나만 고쳤다"였다** — 1회전 수정 과정에서 같은 패턴이 다시 나왔다.
가장 아픈 것은 **README 「강제 방식」 열이 스크립트인 행에 권고(크기 판정)를 끼워 넣은 것**:
CHANGELOG·STATUS·가이드는 "기계 장치 없음"을 정직하게 적었는데 **저장소 첫 화면 표만 반대로 읽혔다**.
반증 하나가 `git diff main HEAD`(커밋된 것만)로 판정해 잘못 기각한 것도 기록했다 — 리뷰 범위는 미커밋 작업 트리다.

**리뷰 처방을 절반 반려했다** (근거는 `issues.md` #059~#063):
코덱스의 `ST-000` 5개 항목 추가는 **4개가 이미 있는 칸의 복제**였고(트리거·결과=1-1절 B칸, 수용 기준=C칸, 요구사항 연결=매핑표 롤업),
*"슬롯 채우기 전에 크기 판정"*은 **순서가 거꾸로**였다. 대신 코덱스가 안 본 **프로파일 경로**를 채웠다 —
Story 문서는 Lite가 안 만들고 Standard도 권한 영향분만 만들므로, `ST-000`에만 걸면 대부분의 경로에서 없는 규칙이 된다.
`C00-template.md` 3곳에 **"문서를 안 만드는 것이지 판정을 건너뛰는 것이 아니다"**를 걸었다.
겸사겸사 남은 것 (b)의 사이클 신호 이중 기재를 닫았다.

**이번 범위 밖으로 남겼던 것** (전부 선재 결함) → **위 §A군 정리에서 전부 닫혔다**:
~~#089 정본 둘~~ · ~~#090 반쪽 리포트~~ · ~~#091 준비 칸 fixture 없음~~ · ~~#077 절 이름 포인터 무검사~~.

## 이 저장소 자신의 코드리뷰를 서브에이전트로 강제 (2026-08-27)

codex 외부 리뷰 수용. 이 저장소의 변경은 커밋 전 `/mdm-kit-review`(축별 서브에이전트 K1~K6 + `mdm-kit-refute` 반증)를 거친다.
정본은 `.claude/commands/mdm-kit-review.md`. 커밋 게이트는 **git 네이티브** `.githooks/pre-commit`·`pre-merge-commit`
(커밋될 트리 == 도장 트리, `review-stamp.sh`의 git write-tree). 활성화: `git config core.hooksPath .githooks`(설정 완료).
자기 리뷰를 3회전 돌려 이슈 #013~#032를 잡고 고쳤다(대부분 닫힘).

**커밋 게이트는 에이전트만 막는다 (2026-08-27 사용자 지적 #033)** — 훅이 사용자 커밋까지 막던 것을 고쳤다.
사용자는 리뷰 지적의 반영 여부를 정하는 권한자이지 통제 대상이 아니다. 이 저장소의 커밋 정책은 **승인 모드**다.

**키트에 커밋 정책 신설 (#034)** — 키트에는 커밋 규칙이 아예 없었다. 비개발자는 "이 지적이 커밋을 막을 만한가"를
판정하기 어려우므로, 판정을 강요하는 대신 **관여 수준을 고르게** 했다: **승인 모드**(묻고 커밋) / **보고 모드**(커밋하고 보고).
S1에서 프로파일과 함께 묻고 `product.md`·`STATUS.md`에 기록한다. **기계 장치는 없다** — 절대 규칙 13(산문)과
S1 DoD의 `(사람 확인)` 항목이 전부이며, 그 사실을 문서에 정직하게 적었다. 모드와 무관하게 유지되는 것:
`/mdm-review` 통과 없이 커밋 금지 · push·배포·태그·**이력 재작성**·되돌릴 수 없는 외부 행동은 반드시 승인.
정본 `docs/guides/commit-policy.md` · 절대 규칙 13.

**2회전 리뷰 완료 (PR #1)**: K1 조건부 · K5 실패(높음 1) → 이슈 #047~#058 전부 닫고 후속 커밋으로 올렸다.
2회전이 잡은 두 가지가 특히 값을 했다: **훅 실행 권한 무검사**(git은 실행 권한 없는 훅을 조용히 건너뛰므로
100644로 커밋되면 게이트가 fail-open — 이제 `check-docs.sh`가 잡는다)와 **#041 결함의 한 층 위 재발**
(키트 3곳은 맞췄는데 루트 `CLAUDE.md`가 짧은 판본이었다 — 이제 세지 않고 정본을 가리킨다).
**교훈: 목록을 복제하면서 "이번엔 맞췄다"고 믿는 것이 이 저장소의 반복 실패 패턴이다.**

**#028 닫힘 — 보조 훅을 넣지 않는다 (2026-08-27 사용자 결정)**: cherry-pick·revert·rebase 재생 커밋은
git이 차단형 pre-* 훅을 주지 않아 도장 없이 HEAD를 전진시킬 수 있다(merge는 `pre-merge-commit`으로 막음).
**문서화된 한계로 둔다.** 근거 셋: (1) 쓸 수 있는 `post-rewrite`는 커밋 뒤에 도는 경고라 게이트가 아니다,
(2) 위협 모델은 "잊고 커밋"인데 rebase는 의도적 행동이라 정책의 영역이다,
(3) **구멍이 스스로 닫힌다** — 도장은 트리 전체 해시라, 그렇게 들어온 코드도 다음 정상 커밋의 도장에 포함되어
그때 `/mdm-kit-review` 대상이 된다. 실질 노출은 rebase 충돌 해결 정도로 좁다.
정직한 한계 목록의 정본은 루트 `CLAUDE.md` 절대 규칙 1 ①~④다 (여기서 다시 세지 않는다 — 세는 순간 갈라진다).
**이 게이트는 잊고 커밋하는 것을 막지, 작정하고 우회하는 것은 못 막는다.**

## 지금 하고 있는 일

0.5.0 작성 완료. **키트가 특정 계획 도구 없이 완결되게 만들었다.**

0.4.0은 상류를 manyfast(MCP)로 전제했다. 사용자 지적: *"지금은 내가 쓸 거니까 manyfast가 있지.
대부분은 manyfast가 없어. 그러니 manyfast로 계획을 세우는 것은 어리석다."*
그리고 실물을 열어 확인하니 **전제가 둘 더 깨졌다**(아래 §결정 근거).
0.4.0은 커밋하지 않고 **0.5.0 하나로 낸다** — `R-###`가 공개 기록에 나가지 않는 편이 낫다.

이번에 넣은 것:

- **진입점을 `/mdm-adopt` 하나로 통합** — 저장소 안을 먼저 뒤지고, 없으면 밖을 묻고, 그래도 없으면 `/mdm-plan`
- **`/mdm-plan` 신설** — 압박(종료 3상태) → 묶어 묻기 → 작성 → 검증. 외부 의존 0. `docs/guides/plan.md`
- **ID는 발급이 아니라 인용** — 키트는 형식을 강요하지 않고, 검사기는 매핑표의 ID에서 접두사를 유도한다
- **검증 조건 수만큼 테스트 강제** — 매핑표 `조건 수` 칸. 조건 5개에 테스트 1개면 완료로 못 올린다
- **계약 갭 표 → 계약 확인표** — 단정하지 않고 판정하며, 그 판정이 정본 소유권까지 정한다
- **묶어 묻기** — S1~S4의 "한 번에 하나씩"을 대체. 정본은 `docs/guides/index.md` 한 곳
- **부작용 지표** — S1 ④와 `/mdm-plan`에 추가 (Standard+)
- **`/mdm-ready` 준비도 점검** — **계층대로 나눴다**: 요구사항=수용 기준(조건 수) · Story=개발 준비 슬롯 12칸.
  공유되는 칸은 정본을 **가리키기만** 하고(두 벌 방지), 이 Story만의 칸만 직접 쓴다.
  **AI가 초안을 채우고 사람은 확인** — 갈리는 것만 질문. 매핑표 `준비` 칸은 Story 롤업이고 미달이면 사이클을 못 연다
- **`domain.md` 5절 비즈니스 규칙 신설** — 도메인 규칙이 저장될 자리가 아예 없었다 (지어내지는 진원지 하나 더)
- **실패 → 해결 경로 연결** — 검사 실패 유형별로 어디로 가는지 표. 검사기 메시지에도 갈 곳을 붙였다
- **판정은 계속된다** — `/mdm-adopt --sync`가 상류 변경을 잡으면 `준비` 칸을 `❌ 재판정`으로 되돌리고,
  계약 확인표도 다시 본다. `/mdm-cycle-close` 13단계가 다음 사이클 준비도를 재확인한다
- **HTML 리포트** — `report.py`가 md를 읽어 한 장으로 그린다. 사람이 판정해야 하는 4시점
  (`/mdm-adopt`·`/mdm-plan`·`/mdm-ready`·`/mdm-cycle-close`). `docs/reports/`, 형상 관리 제외. **정본은 md**
- **테스트 실재 검사(G)** — 매핑표 테스트 칸에 이름만 적으면 개수 검사가 속는 구멍을 막았다.
  문서 속 인용은 실재로 안 친다. 겸사겸사 빈 칸이 bash 3.2에서 검사를 조용히 통과시키던 잠복 버그도 수정
- **설치 끝에 훅 미등록 경고** — `settings.json.dev-kit` 병합 전까지 훅이 조용히 죽는 단일 실패 지점을 마지막 출력으로 크게 알린다
- **계약 확인 설명 일원화** — 판정 어휘와 소유권 귀속의 정본은 `source-map.md` 4절 하나. 커맨드·가이드는 가리키기만 한다 (세 벌 → 한 벌)

핵심 판단은 0.4.0에서 이어진다: **유저플로우는 상태 전이표가 아니다.** 그리고 그것만은
어느 계획 도구도 구조로 담지 않으므로 거의 항상 갭으로 나온다.

## 결정 근거 — manyfast 실물 확인 (2026-08-26)

MCP로 실제 프로젝트를 열어 확인했다 (요구사항 6 · 검증 조건 30 · 기능 14 · 사양 2).

| 발견 | 무엇을 바꿨나 |
|---|---|
| `R-AOTSCK` 형태로 **이미 ID를 발급한다** — 0.4.0의 `R-###`와 접두사 충돌 | ID는 상류 것을 그대로 인용 |
| 요구사항마다 `acceptanceCriteria` 5개 (검증 조건) | 조건 수만큼 테스트 강제 |
| 기능 14개에 `permissions`·`rules`·`dataSpec`이 **채워져 있다** | 갭 표 → 확인표. "상류는 안 담는다"는 전제가 틀렸다 |
| 슬롯 질문에 `options`·`recommendedOption`이 이미 있다 | 묶어 묻기의 절반은 상류가 한다. 키트는 묶기와 순서만 더한다 |
| 성공 지표는 번호 없는 산문 한 덩어리 | 지표는 계획 단계에서만 요구하고 매핑표로 추적하지 않는다 |
| 화면 ID는 발급하지 않는다 (여정은 산문·다이어그램) | 화면 ID는 키트가 부여 |

그리고 **화면 스크린샷 7장**을 보고 API 로는 안 보이던 것이 더 나왔다:

| 발견 | 무엇을 바꿨나 |
|---|---|
| **계층마다 붙는 것이 다르다** — 요구사항=수용 기준 체크박스(0/5), 기능·사양=개발 준비 슬롯 | 준비도를 계층대로 분리 (요구사항=조건 수 / Story=슬롯 12칸) |
| `개발 준비 슬롯 · 9/9` 카운터 + **`AI로 채우기` 버튼** | "9칸은 빈칸 노동"이라는 내 판단이 틀렸다 — **채우는 건 AI, 사람은 확인** |
| 슬롯에 **비즈니스 규칙**이 있다 | `domain.md` 5절 신설 (지금까지 저장될 자리가 없었다) |
| 기능 레벨에도 `기능의 근거`·`성공 기준` 필드 (비어 있어 API 에 안 나옴) | 참고만 — 키트는 요구사항 레벨 수용 기준으로 충분하다고 판단 |

## 다음 3가지

1. **첫 적용** — 실제 제품 저장소에 0.5.0을 설치해 한 사이클 돌리고 `examples/first-pilot` 양식으로 회고를 남긴다.
   검사 9종이 실제 프로젝트에서 무엇을 잡고 무엇을 오탐하는지는 그때 처음 드러난다
   (양식에 진입 경로·계약 확인·/mdm-ready·검사 적발/오탐 칸을 넣어 두었다 — 2026-08-27)
2. 제품 저장소용 CI 워크플로 템플릿 배포 여부 판단 (지금은 `/mdm-review`·`/mdm-cycle-close`가 검사 실행을 담당)
3. **결여 영역 6건(#001~#006) 착수 판단** — 롤백·데이터 마이그레이션(#001)과 보안 절차(#002)가 높음이다.
   각각 프로파일별 배치와 강제 장치(또는 명시적 「권고」 라벨)를 함께 설계해야 한다
   (~~#092 "기록 없이 고치기"~~ → **닫힘**: `mdm-code-review` 검사 17로 들어갔다)

완료: `examples/`·`guides/` 를 0.5.0 구조에 맞춤 (2026-08-27) — getting-started 층 표에 스크립트 층 추가(3층 → 4층),
first-pilot 회고 양식에 진입·계약 확인·검사 칸 추가, 로컬 workflow 리포트 7종 반영

## 열린 이슈

`issues.md`(로컬 전용, 형상 관리 제외) 참조. 공개되는 버전별 변경 기록은 `CHANGELOG.md`가 정본이다.

**2026-08-26 결정** (상세는 `handoff-0.5.0.md` §3-4):

- S1~S4는 **절 단위로 묶어 묻고 추천안 동반**. 순서 계산은 `/mdm-plan`에만. S4 2부(기술 스택)는 예외
- **검증 조건 수만큼 테스트를 강제한다** — 못 세는 상류는 `—`로 내려감
- 성공 지표·**부작용 지표**는 계획 단계에서만 요구하고 매핑표로 추적하지 않는다
- **상류가 준 ID를 그대로 인용한다** — 키트는 형식을 강요하지 않는다
- **계약 갭 표는 단정이 아니라 확인이다** — 판정이 정본 소유권도 정하고, 후보 밖도 줄을 더한다
- **매핑표의 한 행 = 상류 최상위 요구사항** — 검증 조건이 붙어 있는 계층이 행의 단위다
- **준비도는 계층대로 나눈다** — 요구사항=수용 기준, Story=개발 준비 슬롯 12칸. 매핑표 `준비` 칸은 롤업
- **슬롯은 AI가 초안을 채우고 사람이 확인한다** — 빈 칸을 전부 물으면 빈칸 노동이 되어 안 쓰인다
- 외부 도구에서 **방식만 가져오고 용어는 안 가져온다** — 키트 문서는 키트 자신의 말로 쓴다

미해결로 남긴 것:
- 제품 저장소용 CI 워크플로 템플릿은 아직 배포하지 않는다 (검사 실행은 `/mdm-review`·`/mdm-cycle-close`가 담당)

## 자동 검증

- `scripts/check-docs.sh` — 문서 참조·경로(키트 + **이 저장소 자신**)·**절 이름 포인터**·**축↔에이전트 대응**·
  훅/스크립트 권한·문법 검사 (CI: `.github/workflows/docs-check.yml`).
  권한 검사는 `.githooks/*`도 본다 — 실행 권한이 빠지면 git 이 훅을 조용히 건너뛰어 게이트가 fail-open 된다
- `scripts/test-review-gate.sh` — 커밋 게이트 회귀 (뮤테이션 자기검증 포함, CI)
- `scripts/test-consistency.sh` — 정합성 검사 회귀: H 마일스톤 배치 · **준비도 롤업 4분기** · **I 문서 등재 대조** (뮤테이션 자기검증 포함, CI)
- `scripts/test-report.sh` — `report.py` 회귀: Story 문서 ↔ 사이클 축약 슬롯 **공존 모드** (뮤테이션 자기검증 포함, CI)
- `scripts/install-kit.sh` — 설치·병합·업그레이드 (신규/업그레이드/재업그레이드 3경로 실측)
- `templates/dev-kit/.claude/scripts/check-consistency.sh` — 정합성 실측
  (도입 전 → 통과 → 적발 → 경고 → 복구, git 있는 경로와 없는 경로 각각, bash 3.2 포함 — 검사 I 는 fixture 로 실측)

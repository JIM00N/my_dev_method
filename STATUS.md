# STATUS — my_dev_method 저장소 상태

> 이 파일은 방법론 저장소 **자신의** 상태다. 키트가 제품 저장소에 요구하는 것의 최소형(Lite)을 스스로 지킨다.
> 제품 프로젝트의 STATUS는 각 제품 저장소의 `docs/status/STATUS.md`가 소유한다.

**최종 갱신**: 2026-08-26
**프로파일**: Lite (문서 저장소 — 나만 쓰고, 로컬에서만)
**현재 버전**: 0.5.0 (커밋 완료 — v0.5.0 태그 전)

## 지금 하고 있는 일

0.5.0 작성 완료. **키트가 특정 계획 도구 없이 완결되게 만들었다.**

0.4.0은 상류를 manyfast(MCP)로 전제했다. 사용자 지적: *"지금은 내가 쓸 거니까 manyfast가 있지.
대부분은 manyfast가 없어. 그러니 manyfast로 계획을 세우는 것은 어리석다."*
그리고 실물을 열어 확인하니 **전제가 둘 더 깨졌다**(아래 §결정 근거).
0.4.0은 커밋하지 않고 **0.5.0 하나로 낸다** — `R-###`가 공개 기록에 나가지 않는 편이 낫다.

이번에 넣은 것:

- **진입점을 `/adopt` 하나로 통합** — 저장소 안을 먼저 뒤지고, 없으면 밖을 묻고, 그래도 없으면 `/plan`
- **`/plan` 신설** — 압박(종료 3상태) → 묶어 묻기 → 작성 → 검증. 외부 의존 0. `docs/guides/plan.md`
- **ID는 발급이 아니라 인용** — 키트는 형식을 강요하지 않고, 검사기는 매핑표의 ID에서 접두사를 유도한다
- **검증 조건 수만큼 테스트 강제** — 매핑표 `조건 수` 칸. 조건 5개에 테스트 1개면 완료로 못 올린다
- **계약 갭 표 → 계약 확인표** — 단정하지 않고 판정하며, 그 판정이 정본 소유권까지 정한다
- **묶어 묻기** — S1~S4의 "한 번에 하나씩"을 대체. 정본은 `docs/guides/index.md` 한 곳
- **부작용 지표** — S1 ④와 `/plan`에 추가 (Standard+)
- **`/ready` 준비도 점검** — **계층대로 나눴다**: 요구사항=수용 기준(조건 수) · Story=개발 준비 슬롯 12칸.
  공유되는 칸은 정본을 **가리키기만** 하고(두 벌 방지), 이 Story만의 칸만 직접 쓴다.
  **AI가 초안을 채우고 사람은 확인** — 갈리는 것만 질문. 매핑표 `준비` 칸은 Story 롤업이고 미달이면 사이클을 못 연다
- **`domain.md` 5절 비즈니스 규칙 신설** — 도메인 규칙이 저장될 자리가 아예 없었다 (지어내지는 진원지 하나 더)
- **실패 → 해결 경로 연결** — 검사 실패 유형별로 어디로 가는지 표. 검사기 메시지에도 갈 곳을 붙였다
- **판정은 계속된다** — `/adopt --sync`가 상류 변경을 잡으면 `준비` 칸을 `❌ 재판정`으로 되돌리고,
  계약 확인표도 다시 본다. `/cycle-close` 13단계가 다음 사이클 준비도를 재확인한다
- **HTML 리포트** — `report.py`가 md를 읽어 한 장으로 그린다. 사람이 판정해야 하는 4시점
  (`/adopt`·`/plan`·`/ready`·`/cycle-close`). `docs/reports/`, 형상 관리 제외. **정본은 md**
- **테스트 실재 검사(G)** — 매핑표 테스트 칸에 이름만 적으면 개수 검사가 속는 구멍을 막았다.
  문서 속 인용은 실재로 안 친다. 겸사겸사 빈 칸이 bash 3.2에서 검사를 조용히 통과시키던 잠복 버그도 수정
- **설치 끝에 훅 미등록 경고** — `settings.json.dev-kit` 병합 전까지 훅이 조용히 죽는 단일 실패 지점을 마지막 출력으로 크게 알린다
- **계약 확인 설명 일원화** — 판정 어휘와 소유권 귀속의 정본은 `source-map.md` 4절 하나. 커맨드·가이드는 가리키기만 한다 (세 벌 → 한 벌)

핵심 판단은 0.4.0에서 이어진다: **유저플로우는 상태 전이표가 아니다.** 그리고 그것만은
어느 계획 도구도 구조로 담지 않으므로 거의 항상 갭으로 나온다.

## 결정 근거 — manyfast 실물 확인 (2026-08-26)

MCP로 실제 프로젝트를 열어 확인했다 (요구사항 6 · 검증 조건 30 · 기능 14 · 사양 2).

| 발견 | 무엇을 바꿨나 |
|---|---|
| `R-AOTSCK` 형태로 **이미 ID를 발급한다** — 0.4.0의 `R-###`와 접두사 충돌 | ID는 상류 것을 그대로 인용 |
| 요구사항마다 `acceptanceCriteria` 5개 (검증 조건) | 조건 수만큼 테스트 강제 |
| 기능 14개에 `permissions`·`rules`·`dataSpec`이 **채워져 있다** | 갭 표 → 확인표. "상류는 안 담는다"는 전제가 틀렸다 |
| 슬롯 질문에 `options`·`recommendedOption`이 이미 있다 | 묶어 묻기의 절반은 상류가 한다. 키트는 묶기와 순서만 더한다 |
| 성공 지표는 번호 없는 산문 한 덩어리 | 지표는 계획 단계에서만 요구하고 매핑표로 추적하지 않는다 |
| 화면 ID는 발급하지 않는다 (여정은 산문·다이어그램) | 화면 ID는 키트가 부여 |

그리고 **화면 스크린샷 7장**을 보고 API 로는 안 보이던 것이 더 나왔다:

| 발견 | 무엇을 바꿨나 |
|---|---|
| **계층마다 붙는 것이 다르다** — 요구사항=수용 기준 체크박스(0/5), 기능·사양=개발 준비 슬롯 | 준비도를 계층대로 분리 (요구사항=조건 수 / Story=슬롯 12칸) |
| `개발 준비 슬롯 · 9/9` 카운터 + **`AI로 채우기` 버튼** | "9칸은 빈칸 노동"이라는 내 판단이 틀렸다 — **채우는 건 AI, 사람은 확인** |
| 슬롯에 **비즈니스 규칙**이 있다 | `domain.md` 5절 신설 (지금까지 저장될 자리가 없었다) |
| 기능 레벨에도 `기능의 근거`·`성공 기준` 필드 (비어 있어 API 에 안 나옴) | 참고만 — 키트는 요구사항 레벨 수용 기준으로 충분하다고 판단 |

## 다음 3가지

1. **v0.5.0 태그** (0.5.0 커밋·푸시는 완료)
2. `examples/`와 `guides/`(저장소 자신의 해설)가 새 진입점 구조와 맞는지 확인
3. 제품 저장소용 CI 워크플로 템플릿 배포 여부 판단 (지금은 `/review`·`/cycle-close`가 검사 실행을 담당)

## 열린 이슈

`issues.md`(로컬 전용, 형상 관리 제외) 참조. 공개되는 버전별 변경 기록은 `CHANGELOG.md`가 정본이다.

**2026-08-26 결정** (상세는 `handoff-0.5.0.md` §3-4):

- S1~S4는 **절 단위로 묶어 묻고 추천안 동반**. 순서 계산은 `/plan`에만. S4 2부(기술 스택)는 예외
- **검증 조건 수만큼 테스트를 강제한다** — 못 세는 상류는 `—`로 내려감
- 성공 지표·**부작용 지표**는 계획 단계에서만 요구하고 매핑표로 추적하지 않는다
- **상류가 준 ID를 그대로 인용한다** — 키트는 형식을 강요하지 않는다
- **계약 갭 표는 단정이 아니라 확인이다** — 판정이 정본 소유권도 정하고, 후보 밖도 줄을 더한다
- **매핑표의 한 행 = 상류 최상위 요구사항** — 검증 조건이 붙어 있는 계층이 행의 단위다
- **준비도는 계층대로 나눈다** — 요구사항=수용 기준, Story=개발 준비 슬롯 12칸. 매핑표 `준비` 칸은 롤업
- **슬롯은 AI가 초안을 채우고 사람이 확인한다** — 빈 칸을 전부 물으면 빈칸 노동이 되어 안 쓰인다
- 외부 도구에서 **방식만 가져오고 용어는 안 가져온다** — 키트 문서는 키트 자신의 말로 쓴다

미해결로 남긴 것:
- 제품 저장소용 CI 워크플로 템플릿은 아직 배포하지 않는다 (검사 실행은 `/review`·`/cycle-close`가 담당)

## 자동 검증

- `scripts/check-docs.sh` — 문서 참조·경로·훅/스크립트 권한·문법 검사 (CI: `.github/workflows/docs-check.yml`)
- `scripts/install-kit.sh` — 설치·병합·업그레이드 (신규/업그레이드/재업그레이드 3경로 실측)
- `templates/dev-kit/.claude/scripts/check-consistency.sh` — 정합성 실측
  (도입 전 → 통과 → 적발 7종 → 경고 → 복구, git 있는 경로와 없는 경로 각각, bash 3.2 포함)

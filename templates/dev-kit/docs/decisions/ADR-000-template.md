# ADR-000 — <결정 제목>  ← 이 파일은 양식. 복사해서 ADR-001, ADR-002… 로 쓴다

**상태**: 제안 | 채택 | 대체됨(→ ADR-nnn) | 폐기
**날짜**: YYYY-MM-DD
**단계**: S_
**관련**: `docs/spec/<파일>.md`

> 파일명은 `ADR-<3자리 번호>-<주제>.md`, 위치는 이 폴더(`docs/decisions/`)다. 번호는 이어서 증가시킨다.
> 채택된 ADR은 수정하지 않는다 — 그래서 **양식의 플레이스홀더를 남긴 채 채택하지 않는다.**

---

## 배경

무슨 상황에서 이 결정이 필요해졌나. 사실만 적는다.

## 선택지

| 안 | 장점 | 단점 | 비용(바꾸려면) |
|---|---|---|---|
| A | | | |
| B | | | |

## 결정

**<선택한 안>을 채택한다.**  ← 실제 선택으로 바꾼다. 양식의 기본값을 그대로 두지 않는다.

## 근거

왜 그것인가. **그때 알고 있던 것을 기준으로** 적는다.
나중에 상황이 바뀌어 이 결정이 틀리게 되어도, 당시 판단이 합리적이었는지는 이 절로 판단한다.

## 결과

- 이 결정으로 생기는 제약:
- 포기한 것:
- 다시 검토할 시점·조건:

## 되돌리기

- 되돌리는 비용:
- 되돌릴 때 손대야 하는 곳:

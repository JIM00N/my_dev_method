# architecture — 시스템 구조·배치·안정성 (S4 산출물)

가이드: `docs/guides/S4-architecture.md` 1·3·4부
**상태**: 미작성 (미작성 → 작성 중 → 확정) · **최종 갱신**: YYYY-MM-DD

> **코드를 쓰기 전에 이 문서를 읽는다.** 4부의 안정성 대비는 말이 아니라 코드로 존재해야 한다.

---

## 1. 구조

### 통합 / 분리
- 선택:
- 근거:

### 구성 요소
채택 칸은 예/아니오로 적는다.

| 요소 | 채택 | 왜 필요한가 |
|---|---|---|
| 화면(프론트) | | |
| API 서버 | | |
| 데이터베이스 | | |
| 작업 대기열 | | |
| 워커 | | |
| 스케줄러 | | |
| 캐시 | | (측정 전에는 넣지 않는다) |
| 파일 저장소 | | |

### 데이터 흐름
S2 핵심 시나리오를 구성 요소 위에 그린다.

```
사용자 행동 → ... → 저장 → ... → 반응
```

### 어댑터 층
- 어디에 두나:
- 무엇을 감싸나:

### 환경 분리
| 환경 | 데이터 | 브랜치 | 주소 |
|---|---|---|---|
| 로컬 | | | |
| 스테이징 | | | |
| 프로덕션 | | | |

- 환경을 화면에서 구별하는 장치: (배지·색 등 — 무엇을 쓰는지)

## 2. 자동 실행 (배치·스케줄)

| 작업 | 주기 | 하는 일 | 실패 시 | 중복 방지 기준 | 소급 처리 |
|---|---|---|---|---|---|
| | | | | | |

## 3. 안정성 대비 7종 ★★

> **전부 채운다.** 해당 없으면 "해당 없음 + 이유"를 적는다. 빈칸으로 두지 않는다.

> 프로파일 범위: Lite는 1·4·5번만 채운다 (나머지는 `해당 없음(프로파일)`). Standard·Full은 7종 전부. (`docs/guides/profiles.md`)
> 구현됨 칸은 예/아니오로 적는다.

| # | 항목 | 이 프로젝트의 대비 | 구현됨 |
|---|---|---|---|
| 1 | 사용자·권한 (주소 직접 접근, 남의 데이터) | | |
| 2 | 외부 API 변경 (어댑터가 막아 주나) | | |
| 3 | 인증 만료 (감지하나, 알리나) | | |
| 4 | 데이터 오류 (검증 위치) | | |
| 5 | 중복 실행 (같다고 보는 기준) | | |
| 6 | 호출 한도 (외부 한도 / 우리를 향한 과다 호출) | | |
| 7 | 실행 실패 (누가 아나, 재시도 횟수) | | |

## 4. 추가 확인

확인 칸은 예/아니오로 적는다.

| 항목 | 방침 | 확인 |
|---|---|---|
| 비밀값이 형상 관리에서 제외됨 | | |
| 알림 보조 채널 | | |
| 백업 (무엇·주기·위치) | | |
| **복구를 실제로 해 봤는가** (Full은 실연 필수) | | |
| 변경 이력 보관 | | |
| 서버측 입력 재검증 | | |
| 중요 값의 서버측 재계산 | | |

## 5. 잘라낸 것 (과설계 제거 기록)

| 제안된 것 | 왜 뺐나 | 나중에 넣기 쉬운가 |
|---|---|---|
| | | |

# domain — 도메인·데이터·상태 (S2 산출물)

가이드: `docs/guides/S2-domain.md`
**상태**: 미작성 (미작성 → 작성 중 → 확정) · **최종 갱신**: YYYY-MM-DD

---

## 1. 핵심 시나리오

### 정상 흐름
1. (무엇이 시작시키나)
2.
3. (어디서 끝나나 — 성공)

### 분기
| 지점 | 조건 | 어디로 |
|---|---|---|
| | | |

### 예외 흐름 ★
가이드의 5가지 질문에 각각 답이 있어야 한다.

| 상황 | 어떻게 처리하나 |
|---|---|
| 중간에 취소하면 | |
| 필요한 정보가 없으면 | |
| 같은 걸 두 번 하면 | |
| 동시에 건드리면 | |
| 업무시간 밖 / 휴일이면 | |

## 2. 데이터

### 기준 장소 (source of truth)
- 어디가 기준인가:
- 다른 곳에 같은 정보가 있나? 있다면 처리 방침:

### 데이터 목록

#### <데이터 이름>
한 문장 정의:

| 항목 | 형태 | 필수 | 설명 |
|---|---|---|---|
| | 글자/숫자/날짜/참거짓/목록/파일 | 필수/선택 | |

- 무엇으로 구별하나 (고유 식별 기준):
- 다른 데이터와의 관계:

### 관계 요약
| A | 관계 | B |
|---|---|---|
| | 1:1 / 1:N / N:M | |

### 정책
| 항목 | 방침 |
|---|---|
| 삭제 | 진짜 삭제 / 숨김+사유 보관 |
| 변경 이력 | 남김 / 안 남김 · 보관 기간 |
| 기존 데이터 승계 | 어떤 항목을, 어디서 |

## 3. 상태 정의 ★

### 상태 목록
> 8개를 넘으면 2층 구조로 나눈다.

**큰 단계**: (2층 구조인 경우)

| 상태 | 의미 | 이 상태에서 가능한 일 |
|---|---|---|
| | | |

### 상태 전이 표 ★
> 모든 전이에 트리거가 있어야 한다. 트리거 없는 상태는 삭제한다.

| From | To | 트리거 | 누가 | 자동/수동 | 되돌리기 |
|---|---|---|---|---|---|
| | | | | | |

### 자동 전이 조건
| 전이 | 조건 (숫자로) |
|---|---|
| | |

### 최종 상태
- 최종 상태:
- 부활 가능한가:

## 4. 권한

> **이 절을 채우기 전에 계약 확인표를 본다 ★**
> 이 표의 정본은 미리 정해져 있지 않다 — `docs/spec/source-map.md` 4절 계약 확인표의
> 권한 행이 정한다 (1절 정본 소유권 표의 `4절 판정` 항목이다).
>
> | 계약 확인표 판정 | 여기에 무엇을 쓰나 |
> |---|---|
> | `✅ 상류 보유` | **베끼지 않는다.** 상류의 어디인지만 한 줄로 가리키고 아래 표는 비워 둔다 |
> | `❌ 갭` | 아래 표를 채운다. 채우는 순간부터 정본은 이 문서다 |
>
> `/mdm-plan`으로 계획을 만들었으면 계획 문서 2절이 권한을 담으므로 **대체로 `✅ 상류 보유`**다
> (`docs/guides/plan.md` 「정본이 두 벌 되지 않는 이유」). 그때 여기에 표를 다시 채우면
> 같은 것이 두 벌이 되고 상류가 바뀔 때마다 갈라진다.

**정본 위치**: <상류 보유면 어디인지 · 갭이면 「이 문서」>

| 역할 | 조회 | 생성 | 수정 | 삭제 | 남의 것 조회 | 되돌릴 수 없는 행동 |
|---|---|---|---|---|---|---|
| | | | | | | |

> 역할이 5개를 넘으면 과설계를 의심한다.
> 상류가 담는 것은 대체로 **능력 단위**(무엇을 할 수 있나·거부되면 어떻게 되나)다.
> 위 CRUD 격자와 **되돌릴 수 없는 행동** 열은 이 문서가 더하는 형식이므로,
> `✅ 상류 보유`여도 *"되돌릴 수 없는 행동이 무엇인가"*가 상류에 없으면 그 항목만 갭으로 따로 세운다
> (4절 확인표는 후보 목록이지 닫힌 목록이 아니다 — 줄을 더한다).

## 5. 비즈니스 규칙 ★

**"항상 이러해야 한다"는 도메인 약속.** 상태 전이도 권한도 아닌데 구현이 반드시 지켜야 하는 것들이다.

| # | 규칙 | 왜 (근거) | 어기면 무슨 일이 | 어디서 강제하나 |
|---|---|---|---|---|
| 1 | | | | 도메인 경계 / 서버 / DB 제약 |

**여기 적을 것 / 적지 않을 것**

| 적는다 | 다른 절로 보낸다 |
|---|---|
| 어떤 조건이 무엇을 제한하는가 (동의 거부는 *가져오기*가 아니라 *전사 처리*를 막는다) | 상태가 무엇으로 바뀌나 → 3절 전이 표 |
| 무엇을 절대 하지 않는가 (원본 파일을 수정·삭제하지 않는다) | 누가 할 수 있나 → 4절 권한 |
| 계산·판정의 기준 (연락 우선순위는 마지막 접점 + 미해결 약속으로 정한다) | 화면에 어떻게 보이나 → `docs/spec/interface.md` |
| 한도·상한 (한 번에 가져올 수 있는 파일 20개) | 데이터의 형태 → 2절 데이터 목록 |

> **이 절이 왜 따로 있나 ★**
> 규칙은 전이표에도 권한표에도 안 들어가는데, 없으면 구현 중에 정확히 **지어내지는** 종류다.
> *"권한을 거부하면 파일 자체를 못 가져오나, 가져오되 전사만 못 하나?"* — 둘 다 말이 되고,
> 답이 없으면 에이전트가 하나 고른다. 다음 세션에서 다르게 고른다.
>
> **"어기면 무슨 일이" 칸을 비우지 않는다.** 못 적으면 그건 규칙이 아니라 취향이다 — 지운다.

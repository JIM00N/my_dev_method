# 유저플로우 (상류 스냅샷)

**미수집** — `/mdm-adopt`가 상류에서 받아 이 파일을 통째로 대체한다. 손으로 채우지 않는다.

담기는 것: 사용자 흐름과 화면 이동. 화면 ID를 여기서 뽑는다.

> 이것은 **상태 전이표가 아니다.** 화면 이동(A → B)이지 데이터 상태 전이(접수 → 진행중)가 아니다.
> 전이표는 `docs/spec/domain.md`가 소유한다.
